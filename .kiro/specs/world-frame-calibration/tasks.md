# Implementation Plan

> 本計画は `requirements.md`（要件1〜11 / 受入基準74件）と `design.md` に基づく。
>
> **タスク1〜7 は実機を必要としない。** 幾何コア（逆投影・平面推定・マーカー観測・frame 確立・変換・検証・永続化）は
> 実機に依存せず、合成 Depth だけで完成まで到達できる。
> 上流アダプタ（6.1 / 6.2）は `sensing_foundation` の公開型を模した最小ダブルで検証し、
> **実際の疎通と実測はタスク8で行う。**
>
> ⚠️ **ただし幾何コアは `sensing_foundation.geometry` に依存する**（要件 3.8）。
> ピンホール逆投影の基本演算（`depth_raw_to_mm` / `is_valid_depth` / `deproject_pixel`）は
> `sensing-foundation` が所有し、本 Spec と `flying-object-tracking` が**同じ関数を呼ぶ**。
> 当該モジュールは `sensing-foundation` のタスク 1.8 で着地する**状態を持たない純関数3つ**であり、
> 実機も SDK も要求しない。したがってタスク1〜7 が実機不要である性質は変わらないが、
> **タスク 2.1 は `sensing-foundation` タスク 1.8 の完了を前提とする。**
>
> **タスク8 はハードウェア必須**であり、Raspberry Pi 4 と RealSense D435 が揃い、
> かつ `sensing-foundation` の実装が完了するまで着手できない。
> この分割は「実機待ちの期間に何も進まない」事態を避けるための意図的なものである。
>
> ⚠️ **本 Spec が触るファイルは `src/world_frame_calibration/**`、`tests/world_frame_calibration/**`、
> 自 Spec ディレクトリ、およびルート `pyproject.toml` への追記だけである。**
> `flying-object-tracking` が並行して同じ `pyproject.toml` を追記する可能性があるため、
> 既存行を書き換えず追記のみを行う。

## 1. 基盤: パッケージ骨組みと共有型

- [ ] 1. 基盤: パッケージ骨組みと共有型

- [x] 1.1 パッケージ骨組みとテスト実行基盤を用意する
  - `src/world_frame_calibration/` と `tests/world_frame_calibration/` を作成し、既存の `src/` レイアウトに合わせる
  - ⚠️ **前提（着手前に確認する）**: `tests/prediction_core/test_packaging.py::test_no_third_party_runtime_dependencies` は
    当初 `[project].dependencies == []` に加えて **`[project.optional-dependencies] == {}` も表明**しており、
    extras を1つでも新設した時点で、既にマージ済みの `prediction-core` のテストが赤くなる。
    この不変条件の是正（後者を `ALLOWED_OPTIONAL_EXTRAS = {"sensing", "tracking", "calibration", "m1-viz"}` との
    包含関係へ差し替え、前者はそのまま残す）は **`sensing-foundation` のタスク 1.1 が所有する**。
    `calibration` はその許可リストに含まれている。**本 Spec は当該テストを一切変更しない。**
    是正が着地していない状態で下記の追記を行うと既存テストが赤くなるため、着地を確認してから進める
  - ルートの `pyproject.toml` に**追記のみ**を行う: wheel の `packages` に自パッケージを足し、`[project.optional-dependencies]` に `calibration = ["numpy>=1.24"]` を新設する
  - **`[project].dependencies` は空のまま変更しない**（`prediction_core` の実行時依存ゼロを壊さないため）
  - `.gitignore` は編集しない。出力先は `var/calibration/` とし、上流が追加する `var/` の傘に入れる
  - `tests/world_frame_calibration/conftest.py` に一時ディレクトリと既定計画の共通フィクスチャの器を置く
  - 観測可能な完了状態: `import world_frame_calibration` が成功し、`python -m pytest` が既存の `prediction_core` テストとあわせて成功終了する
  - _Requirements: 11.5_

- [x] 1.2 (P) 例外階層と失敗理由の列挙を定義する
  - 基底例外、設定・入力ファイルの誤りを表す例外、**座標系が定まらない条件を表す例外**の3階層を定義する
  - 失敗理由を、有効点不足・平面が支持されない・入射角過小・マーカー未検出・基線長不足・マーカー縮退・非対応の歪み・回転が非直交・未知の形式版・設定不一致・検証点が独立でない、として列挙する
  - 失敗例外に「実測値と判定に用いた下限」を載せる文脈情報の器を持たせる
  - **なぜ値ではなく例外にするのか**（縮退した変換が下流へ流れること自体が事故であるため）を docstring に明記する
  - 観測可能な完了状態: すべての失敗が基底例外として捕捉でき、失敗理由が文字列比較で分岐できることをテストで固定する
  - _Requirements: 5.5, 5.6_
  - _Boundary: Errors_

- [x] 1.3 (P) 線形代数とロバスト統計のユーティリティを実装する
  - 正規化、Z 軸と X 方向ヒントからの右手系正規直交基底の構成、正規直交性の検査、2つの回転の全体角を実装する
  - 各軸の中央値と中央絶対偏差ベースの散らばりを返すロバスト代表値を実装する
  - X 方向ヒントの Z 直交成分がほぼ消える場合を、真偽と大きさとして返す（判定自体は上位が行う）
  - 本モジュールは NumPy 以外を import しない
  - 観測可能な完了状態: 既知の回転に対して構成した基底が右手系・単位長であり、既知角度の回転差が数値誤差の範囲で復元されることをテストで固定する
  - _Requirements: 2.4, 3.3_
  - _Boundary: LinAlg_

- [x] 1.4 値オブジェクトを定義する
  - 内部パラメータ、ストリーム識別情報、画素範囲、Depth 画像、平面と推定品質、マーカー観測、frame 幾何、許容値仕様を、不変（frozen かつ slots）で定義する
  - 距離 mm・角度 deg・画素 px をフィールド名に含める
  - Depth 画像の無効画素は NaN で表し、**0 で埋めない**（欠測と実測値の 0 を区別する）
  - 内部パラメータとストリーム識別情報は**上流のフィールド名をそのまま採る**（写経ミスを防ぐため）
  - 許容値仕様に「暫定か実測由来か」と出典文字列を必須で持たせる
  - frame 幾何にヨー感度（基線長の逆数から導く角度感度と、距離 1000 mm での横方向誤差係数）を持たせる
  - 観測可能な完了状態: 全型が値等価であり、ヨー感度が基線長から解析的に導いた値と一致することをテストで固定する
  - _Requirements: 1.2, 1.6, 2.5, 2.8, 4.6_
  - _Depends: 1.2_
  - _Boundary: GeoTypes_

- [x] 1.5 キャリブレーション計画の読み書きと起動時検証を実装する
  - 床探索範囲、原点マーカーと方向マーカーの範囲・高さ帯、検証点（範囲・高さ帯・既知の World 座標・実測手段）、下限群、許容値、メジャー実測の基線長を1ファイルとして定義する
  - 形式版を持たせ、未知の版を読み込んだら起動時に失敗させる
  - 範囲が画像外・空、高さ帯の下限が上限以上、検証点ラベルの重複を**起動時に**拒否する
  - 下限群の既定値には導出根拠を docstring に併記する（特に基線長下限は「観測誤差 10 mm・距離 2 m で横ずれ約 25 mm」から置いた暫定値であること）
  - 検証点の既知座標は**メジャー実測値**であり、実測手段の記録が必須であることを型で強制する
  - 観測可能な完了状態: 保存 → 読み込みで内容が一致し、不正な範囲・未知形式版・ラベル重複がいずれも読み込み時に拒否されることをテストで固定する
  - _Requirements: 1.4, 4.9, 7.1_
  - _Depends: 1.4_
  - _Boundary: CalibrationPlan_

- [x] 1.6 既知姿勢からの合成 Depth 生成器をテストツリーに用意する
  - 既知のカメラ姿勢・既知の内部パラメータ・既知のマーカー配置から、床平面と直方体マーカーを描画した Depth 画像を生成するヘルパを `tests/world_frame_calibration/synthetic.py` に置く
  - 生成時に**正解の変換**（カメラ → World）を同時に返し、以降のテストが復元結果と直接比較できるようにする
  - 指定した標準偏差のノイズ、指定した割合の無効画素、外れ値（床以外の平面や浮遊物）を混ぜられるようにする
  - 縮退条件（内点不足・浅い入射角・マーカー未配置・短い基線長）を再現する引数を持たせる
  - **パッケージ本体には置かない**（投擲物理・ノイズ生成は `trajectory-simulator` の責務であり本 Spec の責務外）
  - 観測可能な完了状態: 同じ引数で2回生成した Depth 画像が完全に一致する
  - _Requirements: 9.1, 9.5_
  - _Depends: 1.4_
  - _Boundary: tests/world_frame_calibration/synthetic.py_

## 2. 幾何コア: 逆投影・剛体変換・平面推定・マーカー観測

- [ ] 2. 幾何コア: 逆投影・剛体変換・平面推定・マーカー観測

- [x] 2.1 (P) 範囲限定の逆投影と歪みモデルの受理判定を実装する
  - **ピンホールの式を書かない。** 1点ごとの逆投影は `sensing_foundation` の公開入口が提供する
    `deproject_pixel` へ委譲する（要件 3.8）。画素中心規約（`+0.5` を足さない）・mm 換算の位置・
    無効値の定義は上流が持つ規約であり、本 Spec で決め直さない
  - **本モジュールが上流から参照してよいのは `deproject_pixel` と `CameraIntrinsics` の2シンボルだけ**とする。
    自 Spec の内部パラメータ型を上流型へ戻す機械的な写像（フィールド名は同一）をここに1つ置く
  - ⚠️ **これは「上流を import するのは上流アダプタだけ」という当初の制約を意図的に緩めたものである。**
    基本演算は層 L2 の逆投影で必要になり、層 L8 の上流アダプタ経由では層を逆流させずに届かないため。
    緩めた代わりに参照シンボルを上記2つへ限定し、7.3 の境界テストで列挙固定する
  - **歪み係数が厳密に 0 の場合のみ受理し、非ゼロならモデル名と係数を添えて失敗させる**（近似で受理しない）。
    上流は歪み補正を持たず係数 0 を前提に恒等として扱うため、**この番人は本 Spec の責務である**
  - 指定範囲内の有効画素だけを点群として返し、**範囲外の画素に一切触れない**
  - 無効画素（平均化で寄与ゼロになった NaN）を点群から除外する。**この無効点方針も本 Spec の責務である**
    （上流の無効判定は生値に対する述語であり、平均化後の欠測は本 Spec が扱う）
  - 焦点距離が 0 のような未初期化の内部パラメータを設定誤りとして弾く
  - **速度を理由に式をベクトル化して書き直さない。** `m1-prediction-validation` のクロス Spec 契約テスト
    （同 Spec 要件 1.10 / `tests/m1_validation/test_deprojection_contract.py`）が、本 Spec の経路と
    `flying-object-tracking` の経路の**許容差なしの一致**を要求している。必要ならベクトル版は上流へ足す
  - 観測可能な完了状態: 範囲内の各点が上流 `deproject_pixel` の返り値と**完全一致**し、
    非ゼロ歪み係数が失敗すること、範囲外画素と NaN 画素を含まないことをテストで固定する
  - _Requirements: 1.3, 1.4, 3.4, 3.5, 3.8_
  - _Depends: 1.4_
  - _Prerequisite: `sensing-foundation` タスク 1.8（`geometry.py`）_
  - _Boundary: Deprojector_

- [x] 2.2 (P) 剛体変換の保持と適用を実装する
  - 回転行列と平行移動を保持し、単点と点群（N×3）の変換、逆変換、同次行列表現を提供する
  - **構築時に正規直交性と行列式 +1 を検査し、外れれば失敗させる**（拡大縮小・せん断を含まないことを型で守る）
  - 2つの変換の差分を、原点のずれ・全体の回転角・Z 軸のずれ・面内回転（ヨー）のずれに分解して返す
  - **適用の中でログ送出・確保・検証を行わない**（毎フレーム走る唯一の経路であるため）
  - 適用にカメラ・SDK・ファイル入出力を要求しない
  - 観測可能な完了状態: 単点と点群の結果が一致し、逆変換の往復が恒等で、既知の平行移動とヨー回転を差分が分離して返すことをテストで固定する
  - _Requirements: 3.1, 3.2, 3.3, 3.6, 7.4, 11.4_
  - _Depends: 1.3, 1.4_
  - _Boundary: WorldTransform_

- [x] 2.3 床平面推定を実装する
  - 指定範囲の点群に対し、RANSAC で内点を選び、**内点だけで特異値分解による総最小二乗を解き直す**
  - RANSAC の反復回数を成功確率と外れ値率から導出する（固定値を埋め込まない）
  - 法線の向きを**カメラ側が正**に統一する
  - 推定品質として、対象点数・内点数・内点率・残差の中央値と 95 パーセンタイルと二乗平均平方根・使用フレーム数・入射角・使用した乱数シードを返す
  - 平面までの符号付き距離と平面への投影を公開し、マーカー観測と検証が**同じ平面定義**を使えるようにする
  - 有効点不足・内点下限割れ・入射角過小を、それぞれ別の失敗理由として報告する（文脈に実測値と下限を含める）
  - 観測可能な完了状態: 既知平面から生成した点群で法線と距離が復元され、外れ値 40% を混ぜても内点が正しく選ばれ、同一シードで結果が完全に再現することをテストで固定する
  - _Requirements: 1.1, 1.2, 1.3, 1.5, 5.1, 5.4, 9.3, 9.4_
  - _Depends: 2.1_
  - _Boundary: FloorPlaneEstimator_

- [x] 2.4 マーカー観測を実装する
  - 指定範囲の点群のうち、**床平面からの符号付き距離**が高さ帯に入る点だけを選ぶ（画像上の位置や絶対的な奥行きでは判定しない）
  - 選んだ点のロバスト代表値を求め、床平面へ法線方向に投影した点を観測結果とする
  - 代表点の散らばり、点数、床平面からの高さ、カメラからの距離を観測結果に含める
  - **色・柄・既知寸法を要求しない。** マーカーの高さは投影によって結果から消える
  - 点数が下限に満たなければ、どのマーカーかを特定できる形で失敗させる
  - 確立用マーカーと検証点で**同一の関数**を使う（観測方法の違いが検証結果に混入しないようにする）
  - 観測可能な完了状態: 高さ帯外の点が使われないこと、範囲内に外れ値を混ぜても代表点が動かないこと、点数不足がラベル付きで失敗することをテストで固定する
  - _Requirements: 2.2, 2.6, 5.2, 9.3_
  - _Depends: 2.3_
  - _Boundary: AnchorObserver_

## 3. World frame の確立

- [ ] 3. World frame の確立

- [x] 3.1 床平面と2マーカーから World frame を確立する
  - Z 軸を床平面法線（カメラ側正）、原点を原点マーカーの床平面投影点、+X を原点マーカーから方向マーカーへ向かうベクトルの平面内成分、+Y を Z × X として構成する
  - 基線長（2マーカーの床平面上の距離）を算出し、下限を下回れば **+X が不安定である旨とともに失敗させる**
  - 方向マーカーの平面内成分がほぼ消える場合を、別の失敗理由として報告する
  - 基線長からヨー感度を算出し、確立結果に含める
  - **カメラの位置・姿勢を一切参照しない。** マーカーの物理配置だけで frame が決まる
  - 回転行列の各行がカメラ座標系で表した World の各軸であることを docstring に明記する
  - 観測可能な完了状態: 原点マーカーが原点へ、方向マーカーが (基線長, 0, 0) へ写り、床平面上の任意の点の z が数値誤差の範囲で 0 になることをテストで固定する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.7, 3.7, 5.3_
  - _Depends: 2.2, 2.4_
  - _Boundary: WorldFrameBuilder_

- [x] 3.2 カメラ姿勢非依存性と縮退分岐を回帰テストで固定する
  - 合成生成器でカメラ姿勢だけを変え、マーカー配置が同じなら**同一の World 座標が得られる**ことを確かめる
  - 内点不足・浅い入射角・マーカー未検出・基線長不足・マーカー縮退・非対応の歪みの各分岐を、実機を用いずに再現する
  - 各失敗が「どの下限に対してどの実測値だったか」を文脈から読み取れることを確かめる
  - ヨー感度が基線長の逆数から解析的に導いた値と一致することを確かめる
  - 観測可能な完了状態: カメラ姿勢を3通り変えた合成入力で World 座標の差が数値誤差の範囲に収まり、縮退6分岐がすべて意図した失敗理由で失敗する
  - _Requirements: 2.8, 2.9, 9.5_
  - _Depends: 3.1_
  - _Boundary: tests/world_frame_calibration/test_frame.py_

## 4. キャリブレーション結果の永続化と整合性

- [ ] 4. キャリブレーション結果の永続化と整合性

- [x] 4.1 キャリブレーション結果の組み立てと直列化を実装する
  - 形式版、一意識別子、生成時刻、入力元種別と再生対象、ストリーム識別情報、内部パラメータ、平面と推定品質、変換と frame 幾何、2つのマーカー観測、使用した計画の要約、備考を1つの結果として組み立てる
  - JSON へ保存・読み込みし、**保存 → 読み込みで同一の変換が再現される**ようにする
  - 直列化は非数値を許さず、欠測はキーごと省く（0 で埋めない）
  - 検証結果は未付与の状態で保存され、その状態が判別できるようにする
  - 出力先の既定を `var/calibration/` とする
  - 観測可能な完了状態: 保存 → 読み込みした変換が元の変換と数値的に一致し、未検証状態が明示的な値として読み取れることをテストで固定する
  - _Requirements: 6.1, 6.2, 6.6_
  - _Depends: 3.1_
  - _Boundary: CalibrationResultStore_

- [x] 4.2 読み込み時の整合性検査を実装する
  - 形式版が未知なら失敗させる
  - 読み込んだ回転の正規直交性を検査し、外れれば失敗させる
  - 保存された解像度・fps・Depth スケール・Color 有無・内部パラメータの主要値を現在の入力元のそれと比較し、不一致なら**変換を有効なものとして扱わずに**失敗させる
  - **解像度が変われば内部パラメータも変わるため、古い結果の使い回しが静かにずれる**旨を docstring に明記する
  - 検証要約を結果へ付与する操作を、元の結果を変更せず新しい値を返す形で提供する
  - 検証状態を未検証・合格・不合格・未判定の4状態として提供する
  - 観測可能な完了状態: 解像度違いのストリーム識別情報で不一致が失敗として検出され、検証要約の付与後に検証状態が変わることをテストで固定する
  - _Requirements: 6.3, 6.4, 6.5, 6.6, 7.3_
  - _Depends: 4.1_
  - _Boundary: CalibrationResultStore_

- [x] 4.3 (P) 結果どうしの比較による再現性確認を実装する
  - 2つのキャリブレーション結果の変換差分を、原点のずれ・全体の回転角・Z 軸のずれ・ヨーのずれとして返す
  - **Z 軸のずれは平面推定の再現性、ヨーのずれはマーカー設置の再現性**を表すことを docstring に明記する
  - 比較対象のストリーム識別情報が異なる場合は、比較の前に不一致として報告する
  - 観測可能な完了状態: 既知の平行移動とヨー回転だけを与えた2結果に対して、差分がその量を分離して返すことをテストで固定する
  - _Requirements: 7.4_
  - _Depends: 4.1_
  - _Boundary: CalibrationResultStore_

## 5. 独立検証 ★本 Spec の中心

- [ ] 5. 独立検証

- [x] 5.1 検証点の World 座標算出と独立性の検査を実装する
  - 保存済みの結果と検証点の観測から、各検証点の World 座標を算出し、既知の位置との差分を軸ごとに求める
  - **検証を、物体検出・追跡・予測のいずれも呼ばずに完了できる形にする**（入力は結果と観測だけ）
  - 確立に使ったマーカーと重複する検証点を**集計から除外し、重複として明示する**
  - 独立な検証点が1つも無ければ、検証として成立しないものとして失敗させる
  - **確立に使った点は定義上一致するため検証にならない**旨を docstring に明記する
  - 既知の高さを持つ検証点（床上でない点）も同じ形式で扱えるようにする
  - 観測可能な完了状態: 確立用マーカーを検証点に混ぜると除外され、独立点ゼロで失敗し、床上の点と高さのある点の双方で軸ごとの差分が得られることをテストで固定する
  - _Requirements: 4.1, 4.2, 4.3, 4.9_
  - _Depends: 4.2_
  - _Boundary: Verifier_

- [x] 5.2 誤差の分解とスケールの独立確認を実装する
  - 独立点のみから、軸ごとの平均オフセット（バイアス）と、バイアスを除去した残差の二乗平均平方根（ばらつき）を算出する
  - カメラからの距離帯ごとに点数・平均誤差・最大誤差を集計する
  - 計画に記録されたメジャー実測の基線長と、算出した基線長を突き合わせる（**この距離は frame の定義に使っていないため独立な検証量になる**）
  - 最大誤差を全体・水平・垂直に分けて算出する
  - **バイアス支配なら座標系、ばらつき支配なら観測、遠方だけ大きいなら Depth の距離特性**という読み分け規則を docstring に明記する
  - 観測可能な完了状態: 既知のバイアスを注入した検証点に対してバイアスがその量を指し、ばらつきが小さいまま保たれることをテストで固定する
  - _Requirements: 4.4, 4.5, 9.3_
  - _Depends: 5.1_
  - _Boundary: Verifier_

- [x] 5.3 許容値による合否判定を実装する
  - 許容値が与えられた場合、点ごとと全体の合否を判定する
  - **全体の合否は「独立点すべてが水平・垂直の許容値を満たすこと」とし、平均では判定しない**（1点だけ外れる状態を見逃さないため）
  - 許容値が与えられない場合は未判定として扱い、**実測値そのものは無条件に出力する**
  - 判定に用いた許容値と、それが暫定か実測由来かをレポートに必ず含める
  - 対象となる結果の識別子・形式版・ストリーム識別情報をレポートに含める
  - 検証レポートから、結果へ付与するための要約を生成する
  - 観測可能な完了状態: 許容値ありで合格・不合格が正しく分かれ、許容値なしで未判定になり、いずれの場合も実測値がレポートに残ることをテストで固定する
  - _Requirements: 4.6, 4.7, 4.10_
  - _Depends: 5.2_
  - _Boundary: Verifier_

- [x] 5.4 レポート出力（人間可読・機械可読）を実装する
  - 検証レポート・キャリブレーション結果・比較差分のそれぞれに、機械可読の辞書表現と人間可読の要約テキストを用意する
  - 要約テキストの**先頭3行に判定・最大誤差・バイアス**を置く
  - 要約テキストに、判定に用いた許容値と**暫定である旨**を必ず出す
  - **計測した数値を、実測前の目標値との比較による合否判定に使わない**方針を出力上でも守る（暫定表示を落とさない）
  - 機械可読出力は非数値を許さず、欠測はキーごと省く
  - 本モジュールは上流にもロギングにも依存しない（ログ無効時にレポートが変わらないようにする）
  - 観測可能な完了状態: 暫定許容値で判定したレポートの要約に「暫定」が現れ、機械可読出力を再読み込みして同じ値が復元できることをテストで固定する
  - _Requirements: 4.8, 6.5, 10.6_
  - _Depends: 5.3_
  - _Boundary: Reporter_

## 6. 上流接続・計測・入口

- [ ] 6. 上流接続・計測・入口

- [x] 6.1 上流アダプタ（フレーム収集・平均化・型写像）を実装する
  - `sensing_foundation` の**公開入口だけ**からフレーム列を受け取り、指定枚数を収集する
  - **上流の副作用を伴う機能（取得・ロギング・設定解決）を使うのは本モジュールだけである。**
    幾何コアからの上流参照は 2.1 の逆投影プリミティブ2シンボルに限られる
  - 上流のストリーム設定と内部パラメータを自 Spec の値オブジェクトへ写像する（内部パラメータを独自の固定値で持たない）
  - 複数フレームを平均化して Depth 画像を作る。**新しい浮動小数配列へ確保し、上流の読み取り専用 Depth を書き換えない**
  - **生カウントから mm への換算と無効画素の判定は、上流の `depth_raw_to_mm` / `is_valid_depth` を呼ぶ**（要件 3.8）。
    `raw * depth_scale_mm` を自分で書かず、「生値 0」も自前の条件式で判定しない（無効値の定義は上流が持つ）
  - 無効画素を平均から除外し、寄与ゼロの画素は欠測（NaN）として残す。寄与枚数と使用フレーム数を保持する
  - 収集中にストリーム設定が変化した場合は設定誤りとして失敗させる
  - 入力元の選択（live / recorded / simulated）は上流に委ね、**本 Spec でアダプタを新設しない**
  - 観測可能な完了状態: 上流公開型を模した最小ダブルで、入力 Depth 配列が変更されていないこと、同一フレーム列から同一の Depth 画像が得られること、使用枚数が記録されることをテストで固定する
  - _Requirements: 1.3, 1.6, 1.7, 3.8, 8.1, 8.3, 8.4, 8.5_
  - _Depends: 1.4_
  - _Boundary: UpstreamAdapter_

- [ ] 6.2 計測点とロギング委譲を実装する
  - 上流の構造化ロギングへ、**段階名 `calibrate`** で計測値を送出する（基盤・形式・送出機構を自前で作らない）
  - フレーム収集・平面推定・マーカー観測・frame 確立・検証の各段の所要時間と主要な品質値をイベントとして送る
  - 失敗もイベントとして残す（**成功時しかログが残らない状態を作らない**）
  - 変換適用の所要時間を下流が自区間として計測できる薄い委譲を提供する（**変換の実装側には副作用を入れない**）
  - ロギングが無効な場合、計測値の生成そのものを行わない
  - 予約段階名（`system` / `capture` / `record`）と衝突させない
  - 観測可能な完了状態: 無効なロガーを渡したときにイベント生成が行われないこと、各段のイベント名とキーが設計どおりに出ることをダブルで固定する
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_
  - _Depends: 6.1_
  - _Boundary: UpstreamAdapter_

- [ ] 6.3 公開 API を確定する
  - 下流が参照する入口を1箇所に集約し、再エクスポートのみとしてロジックを持たせない
  - 下流が日常的に使う最小集合（結果の読み込み・結果型・変換型・整合性検査・失敗例外・失敗理由）を明示する
  - **`prediction_core` を import しない。** Throw Record スキーマを再定義しない
  - 公開契約に含まれるシンボルを列挙として固定し、そこに無いものは内部実装として扱う旨を docstring に書く
  - 観測可能な完了状態: 公開シンボル一覧がテストで固定され、`sensing_foundation` を未導入の環境でも公開入口の import が成功する
  - _Requirements: 3.6, 8.6_
  - _Depends: 5.4_
  - _Boundary: PublicApi_

- [ ] 6.4 CLI を実装する
  - `calibrate`（収集 → 平面推定 → マーカー観測 → 確立 → 保存）、`verify`（検証と合否）、`show`（要約表示と整合性検査）、`compare`（2結果の差分）を提供する
  - 共通オプションとして計画ファイル、入力元、再生対象、収集枚数、出力先、ログ設定を受け取り、設定の解決は上流に乗せる
  - `verify` は未判定を成功終了、不合格を非ゼロ終了とする（**判定していないことと不合格を区別する**）
  - 失敗は理由と文脈を標準エラーへ必ず出す
  - ヘルプに「既定の下限・許容値はすべて暫定であり実測で見直す」旨を明記する
  - 実機・SDK が無い環境でも、記録済みセッションと合成入力に対して全サブコマンドが動くこと
  - 観測可能な完了状態: 4サブコマンドが合成入力に対して期待どおりの終了コードと出力を返すことをテストで固定する
  - _Requirements: 4.1, 7.1, 7.3, 8.5, 10.3_
  - _Depends: 6.3_
  - _Boundary: CLI_

## 7. 検証・回帰・手順書

- [ ] 7. 検証・回帰・手順書

- [ ] 7.1 合成データによる end-to-end 検証を実装する
  - 既知のカメラ姿勢・既知のマーカー配置から生成した Depth に対し、計画 → 確立 → 保存 → 検証を通す
  - 復元された変換が**正解の変換と数値誤差の範囲で一致する**ことを確かめる
  - 検証が合格になることを確かめる
  - 既知の量のノイズを加えた入力で、平面の残差と検証のばらつきが**ノイズ量に応じて増える**ことを確かめる
  - 検証の実行にハードウェアの接続を要求しないことを確かめる
  - 観測可能な完了状態: 合成入力に対する end-to-end が合格で完了し、ノイズ量を2段階変えたとき品質値が単調に悪化する
  - _Requirements: 9.1, 9.2, 9.3_
  - _Depends: 6.4_
  - _Boundary: tests/world_frame_calibration/test_e2e_synthetic.py_

- [ ] 7.2 検証が不合格を出せることを確かめる
  - 床平面を意図的にずらす、原点マーカーの観測をずらす、といった**既知のバイアスを注入**した入力を作る
  - 検証が不合格になり、平均オフセットが注入した量を指すことを確かめる
  - ばらつきが小さいまま保たれ、**バイアスとばらつきが分離できている**ことを確かめる
  - 遠方の検証点だけ誤差を大きくした入力で、距離帯集計がそれを示すことを確かめる
  - **「検証が本当に不合格を出せる」ことの確認が本 Spec の存在意義そのもの**である旨をテストの docstring に書く
  - 観測可能な完了状態: 注入した3種類の異常がそれぞれ不合格として検出され、レポートの読み分け規則どおりの内訳になる
  - _Requirements: 4.4, 4.5, 4.6_
  - _Depends: 7.1_
  - _Boundary: tests/world_frame_calibration/test_verify.py_

- [ ] 7.3 境界テストで依存方向と禁止 import を静的に固定する
  - 上流アダプタ・逆投影モジュール・CLI 以外のモジュールが `sensing_foundation` を import していないことを静的に検証する
  - **逆投影モジュールが上流から参照するシンボルが `deproject_pixel` と `CameraIntrinsics` の2つだけである**ことを列挙で固定する（要件 3.8）
  - **本パッケージのどこにもピンホールの式が再実装されていない**ことを静的に検証する（`ppx_px` / `fx_px` を用いた除算がソースに現れない）
  - 全モジュールが `cv2` / `pyrealsense2` / `prediction_core` を import していないことを静的に検証する
  - 設計で定めた層をまたぐ逆方向の import が無いことを静的に検証する
  - 変更対象が自パッケージ・自テスト・自 Spec ディレクトリに閉じていることを確認する（`src/prediction_core/**`、`tests/prediction_core/**`、`src/sensing_foundation/**` を変更していない）
  - **検出・追跡・予測に相当する処理を本パッケージが持たない**ことを、公開シンボルと依存の両面から固定する
  - 観測可能な完了状態: 境界テストが単独で実行でき、意図的に違反 import を足すと失敗することを確認できる
  - _Requirements: 3.8, 8.2, 8.6, 11.1, 11.2, 11.4, 11.5_
  - _Depends: 6.3_
  - _Boundary: tests/world_frame_calibration/test_boundaries.py_

- [ ] 7.4 設置手順書を作成する
  - `.kiro/specs/world-frame-calibration/procedure.md` に、物理準備（マーカーの置き方、基線長の目安と下限の理由、床探索範囲の取り方、検証点の配置とメジャー実測の記録方法）を書く
  - 計画ファイルの作り方と保管（**設置ごとに作り直す**）を書く
  - 実行順序（確立 → 整合性確認 → **検証は必須** → 合否の読み方）を書き、検証を経ていない結果を運用に使わないことを明記する
  - レポートの読み分け規則（バイアス／ばらつき／距離帯）を書く
  - 再現性の確認手順（2回実施して比較し `measurements.md` へ記録）を書く
  - **移動体オドメトリ原点の初期化手順**（World 原点は原点マーカーの位置であり、移動体をそこへ置いて +X へ向ければ一致する）を書き、**実装は M3 側で本 Spec の範囲外**であることを明記する
  - 再実施が必要な条件（カメラの移動、設置形態の変更、解像度・fps の変更、マーカー位置の変更、床の変更）を書く
  - 観測可能な完了状態: 手順書だけを読んだ人が、計画の作成から検証の合否判定までを実行できる状態になっている
  - _Requirements: 7.2, 7.3, 7.5, 7.6, 11.3_
  - _Depends: 6.4_
  - _Boundary: .kiro/specs/world-frame-calibration/procedure.md_

## 8. 実機での確立と検証（ハードウェア必須）

> ⚠️ **ここから先は Raspberry Pi 4 と RealSense D435、および `sensing-foundation` の実装完了が前提である。**
> タスク1〜7 の完了はこの前提を必要としない。

- [ ] 8. 実機での確立と検証

- [ ] 8.1 実機で床平面推定と World frame 確立を成立させる
  - 実際の床とマーカーを用いて、上流の live 入力から計画に沿った確立を実行する
  - 推定品質（内点数・内点率・残差・入射角）と基線長・ヨー感度を `measurements.md` に記録する
  - 床探索範囲に壁・天井が入って誤った平面を掴む事象の有無を確認し、範囲の取り方を手順書へ反映する
  - 収集枚数を変えたときの推定品質の変化を記録する
  - 観測可能な完了状態: 実機の live 入力で確立が成功し、採用した計画と推定品質が `measurements.md` に残る
  - _Requirements: 1.1, 1.2, 1.6, 2.7, 2.8, 8.1_
  - _Depends: 7.4_

- [ ] 8.2 実機で独立検証を実施し、誤差を記録する
  - メジャー実測した検証点を3点以上（うち1点は想定投擲距離の端）配置し、検証を実行する
  - 平均オフセット・ばらつき・距離帯ごとの誤差・スケール確認の結果を `measurements.md` に記録する
  - 記録した実データセッションに対して**実機なしで同じ検証が再現できる**ことを WSL 上で確認する
  - 観測可能な完了状態: 実機での検証レポートと、同一セッションを WSL で再生した検証レポートの主要値が一致する
  - _Requirements: 4.2, 4.4, 4.5, 4.10, 8.5_
  - _Depends: 8.1_

- [ ] 8.3 再現性を実測し、暫定の許容値と下限を見直す
  - 同一のマーカー配置で確立を2回以上実施し、比較差分（原点のずれ・Z 軸のずれ・ヨーのずれ）を記録する
  - マーカーを置き直したときの再現性と、置いたままでの再現性を分けて記録する
  - 実測した誤差と再現性に照らして、許容値・基線長下限・内点下限・入射角下限の**暫定値を見直す**
  - 見直した値と、**それが実測由来であること**を計画の許容値仕様と `measurements.md` に反映する
  - ⚠️ 実測前の暫定値を根拠に方針を変えない。見直しは実測後に行う
  - ⚠️ **許容値の更新は Revalidation Trigger である。** `m1-prediction-validation` の `seam.open_calibration()` は
    非 PASS のキャリブレーション結果に対して（`allow_unverified` を明示しない限り）
    `SeamFailure(CALIBRATION_NOT_VERIFIED)` を送出し、
    **M1 の計測実行そのものを止める**。すなわち本 Spec の許容値は、境界を越えて下流の実行可否を握っている
    唯一の数値である。締める／緩めるいずれの方向でも `m1-prediction-validation` へ通知し、
    向こうの実行可否が変わらないことを確認する（m1 側は失敗メッセージに `provisional` フラグと
    `source` 文字列を出すため、運用者は「校正が本当に悪い」のか「暫定許容値が厳しすぎる」のかを区別できる）
  - 観測可能な完了状態: 再現性の実測値と、見直し後の許容値・下限およびその根拠が `measurements.md` に残り、許容値仕様が暫定でなくなる。許容値を変更した場合は `m1-prediction-validation` への通知が記録されている
  - _Requirements: 4.6, 7.4, 10.6_
  - _Depends: 8.2_

## Implementation Notes

- タスク1.2: `CalibrationConfigError` は `prediction_core.PredictionConfigError` / `sensing_foundation.SensingConfigError` と異なり `ValueError` を継承しない。design.md の例外契約スニペットが `ValueError` に一切触れていないため契約どおりに実装した結果であり、実装上の欠陥ではない。今後 design.md を改訂する場合は、他パッケージとの `except ValueError` 互換性を意図的に持たせるかどうかを判断すること。
- タスク1.3: `linalg.orthonormal_basis` は `x_hint` が `z_axis` とほぼ平行（縮退）な入力で呼ばれると `unit()` 内で 0/0 による NaN を静かに返す（例外を送出しない。本モジュールは numpy 以外を import せず判定を持たないため設計どおり）。**タスク3.1（WorldFrameBuilder）は `orthonormal_basis` を呼ぶ前に必ず `x_hint_degeneracy` で縮退を確認し、縮退時は `CalibrationFailure(ANCHOR_DEGENERATE)` を送出してから `orthonormal_basis` を呼ばないこと。** この呼び出し順序を守らないと縮退した基底が静かに下流へ流れる。
- タスク1.4: `FrameGeometry.yaw_sensitivity_deg_per_mm` / `lateral_error_mm_per_mm_at_1000mm` は純粋な値オブジェクトのフィールドであり、`types.py` 自体は計算しない（生成時に検証・導出をしない設計方針どおり）。**この解析的関係を実際に計算して埋めるのはタスク3.1（WorldFrameBuilder）である。** 1.4 のテストは独立に事前計算したリテラル値でこの関係を固定しているが、実装側の計算はまだ存在しないため、3.1 で計算式を書く際に同じ式（`degrees(1/baseline_mm)`、`1000/baseline_mm`）を使うこと。
- タスク1.6: `tests/world_frame_calibration/synthetic.py` は `tests/sensing_foundation/synthetic.py` と**裸のモジュール名が衝突する**（`tests/**` に `__init__.py` が一切無いため）。design.md がファイル名を `synthetic.py` に固定しているためリネームでは解決できない。**このモジュールを他のテストファイルから使う場合は、素の `import synthetic` / `from synthetic import ...` を使わず、`importlib.util.spec_from_file_location` によるパス指定ロードを使うこと**（`test_world_frame_calibration_synthetic.py` に実装パターンあり）。この衝突が今後関係するタスク: 2.x の `test_frame.py`、3.2、7.1 の `test_e2e_synthetic.py`、7.2 の `test_verify.py`。
- タスク2.2: `WorldTransform` 構築時の正規直交性・行列式チェック失敗は `CalibrationFailure(reason=FailureReason.ROTATION_NOT_ORTHONORMAL, context={...})` であり、`CalibrationConfigError` ではない（design.md の Preconditions を正とする）。**形状不一致（3x3でない等）だけは `CalibrationConfigError`。** 縮退条件は `CalibrationFailure` 系、呼び出し方の誤りは `CalibrationConfigError` 系という区別を以降のタスクでも踏襲すること。`apply()` は設計どおり検証を一切行わない（tasks.mdの明示的指示がdesign.mdの矛盾する記述に優先する）。
- タスク3.1: `linalg.x_hint_degeneracy` は `build_world_frame` 内で「原点マーカー→方向マーカー」ベクトルに対して呼ばれるが、**両マーカーの `point_on_plane_mm` は既に同一平面（同じ `Plane`）へ投影済み（タスク2.4の契約）のため、その差ベクトルは常に厳密に平面内（Z直交）になる**。したがって `x_hint_degeneracy` が実際に発火しうるのは「`point_on_plane_mm` が実は渡された `plane` と同一平面上に無い」という不整合入力の場合のみであり、design.md の例示（「マーカーがほぼ真上/真下にある」物理的縮退シナリオ）は実際には基線長不足（`ANCHOR_BASELINE_TOO_SHORT`）側で捕捉される。**タスク3.2で `ANCHOR_DEGENERATE` を合成的に再現する場合、カメラ姿勢パイプライン全体ではなく、直接 `AnchorObservation` を不整合な値で構築する必要がある**（`test_world_frame_calibration_frame.py` の該当テストが実例）。design.md 側のこの記述は将来の改訂で是正が必要。
- タスク5.1: `evaluate_verification_points` は検証点の `AnchorObservation.point_camera_mm`（投影前のロバスト代表点）を使う。**`point_on_plane_mm`（床平面投影後）を使うと高さ情報が失われ、要件4.9（既知の高さを持つ検証点）が満たせなくなる。** 幾何的には、`WorldTransform` の Z 軸＝床平面法線であるため、`apply_point(point_camera_mm).z` は平面からの符号付き距離（＝真の高さ）と厳密に一致し、X/Y成分は投影の有無で変わらない（レビューで解析的に検証済み）。`PointVerification` は design.md の `PointError` の部分集合（bias/scatter/距離帯/tolerance/verdict は含まない）であり、これらはタスク5.2/5.3で追加される。関数名 `evaluate_verification_points` も design.md の最終形 `verify_calibration` とは異なる（tolerance/expected_baseline_mm を束ねるのはタスク5.3以降）。
