# ブリングアップ手順書（teleop-bringup タスク 8.1）

> **この文書が作業中の唯一の拠りどころである。** 会話や記憶に頼らず、ここだけを見て
> 各段を実施し、合否を判定できることを目的とする（要件 10.1, 10.8, 10.9）。
>
> **成果物**: 本手順書と、記録先 [`bringup-log.md`](./bringup-log.md)。
> 実施のたびに log へ追記する（上書きしない）。

---

## 0. 現在地

⚠️ **作業を再開するときは、まずここを読む。実施のたびに更新する。**

### 0.1 いま何が終わっていて、何で止まっているか

| 区分 | 状態 |
|---|---|
| Spec のタスク 1〜7（ビルド構成・端子割当・結線表・アダプタ・入力変換・無線・制御ループ・BenchApp） | ✅ 完了 |
| タスク 2.3（回路図） | 🔶 作図入力まで完了（[`schematic-draft.md`](./schematic-draft.md) / [`schematic-draft.html`](./schematic-draft.html)）。成果物の `schematic.svg` は Cirkit Designer で作図中 |
| タスク 8.1（本手順書） | ✅ この文書 |
| タスク 8.2 以降 | ⬜ 未着手 |
| **案B のファーム実装** | 🔶 **書き上げ済み・未コンパイル**（`firmware/src/teleop/motor_ledc.{hpp,cpp}`） |
| **ビルド環境** | ⛔ **ESP-IDF ツールチェーン未導入。VSCode の PlatformIO 拡張を入れ直す予定** |

### 0.2 ⛔ いまのブロッカー

**ESP-IDF ツールチェーンが入っていない。** `~/.platformio/packages/` の
`tool-cmake` / `tool-ninja` / `toolchain-xtensa-esp-elf` / `tool-esptool` 等が
中身の無い殻で、`.piopm`（導入済みの印）だけが残っているため PlatformIO が取り直さない。
`~/.espressif` も存在しない。

**解消手順**: `~/.platformio` をフォルダごと削除 → VSCode の PlatformIO 拡張を導入 →
`firmware/` で `pio run -e bench`。初回はツールチェーン約1GB と Bluepad32 / BTstack の
取得が走り、20分程度かかる。

⚠️ **その初回ビルドには、`firmware/scripts/fetch_bluepad32.py` の Windows 対応2件が必須である**
（本リポジトリは `core.autocrlf=true` の環境にある）。素のままだと必ず次で止まる。

| 症状 | 原因 | 対応 |
|---|---|---|
| `error: corrupt patch at line 20` | クローン時に BTstack のパッチが CRLF 化する | `git clone` に `-c core.autocrlf=false -c core.eol=lf` |
| `PermissionError: [WinError 5]` | git のパックファイルが読み取り専用で `shutil.rmtree` が消せない | 読み取り専用ビットを落として再試行する `onerror` ハンドラ |

いずれも修正済みで、Bluepad32 の取得とパッチ適用まで通ることは確認済みである。

### 0.3 確定済みの設計判断（実施中に迷ったらここを見る）

| 事項 | 決着 | 根拠の所在 |
|---|---|---|
| IN1/IN2 の駆動方法 | **案B**（ファーム側で直接駆動。前進=IN1にPWM/IN2はL、後進=逆、ゼロ=両方L） | [`schematic-draft.md`](./schematic-draft.md) §7.1 |
| 電流上限 | **1.5A**（VREF=3.3V ＋ RRS=0.22Ω）。⚠️ データシートの要求ではなく**安全側に寄せた判断** | 同 §4.2.3 |
| 5V の切り離し | **JP1**（USB 給電時は外す） | 同 §2.2.1 |
| C3–C5 の個数 | 3個（モータ1個につき赤−白間に1個） | 同 §4.1 |
| エンコーダのプルアップ | 破線・**未実装**の予備位置のみ | 同 §7.3 |
| MCU | **classic ESP32（ESP32-WROOM-32）**。⚠️ ESP32-S3 では端子も BT Classic も成立しない | 同 §7.2 |

### 0.4 未確定のまま進めるもの

⚠️ **確定したように扱わない。** 実測で決まる。

| 事項 | いつ決まるか |
|---|---|
| エンコーダ A相/B相 の線色（黄 or 緑） | E-5（メーカーも示していない） |
| `OUT1`/`OUT2` と 赤/白 の対応 | E-3 |
| 主電源スイッチ SW1 / 電源分岐端子 TB1 | 機構側（OQ-11 / OQ-12）。図では暫定扱い |
| 入力マッピングと初期パラメータ | E-6 以降（OQ-17） |
| コントローラ接続方式の成立可否 | E-5（OQ-16。落ちたら PC 経由へ退避） |

---

## 1. 全体の流れ

⚠️ **順序を入れ替えない。1段ごとに未知が1つだけ増えるように並べてある**（要件 10.1）。
⚠️ **合格条件を満たさなかった段があれば、次の段へ進まない**（要件 10.8）。

| 段 | 内容 | Spec | 電源 | 整備スタンド | 利用者の言い方 |
|---|---|---|---|---|---|
| **E-0** | 無通電で線の色と系統を確かめる | 8.2 | なし | 不要 | — |
| **E-1** | 電流を制限できる電源で導通と短絡を確かめる | 8.3 | 安定化電源 | 不要 | — |
| **E-2** | ドライバを介さずモータ単体を回す | 8.4 | 安定化電源 | 不要 | — |
| **E-3** | 可変抵抗で開ループ確認（モータ1個） | 8.5 | 安定化電源 | 不要 | **step 1** |
| **E-4** | モータ非接続でコントローラの疎通を確かめる | 8.6 | 安定化電源 or USB | 不要 | **step 2 の前半** |
| **E-5** | コントローラで輪単体を回し、エンコーダを校正する | 9.1 (M2a-0) | 安定化電源 | 不要 | **step 2 の後半** |
| **E-6** | モータ3個を机上で回す | 8.5 の延長 | 安定化電源 | 不要 | **step 3** |

🔶 接地走行（逆運動学の符号確認、タスク 9.2 / M2a-1）は**本手順書の範囲外**である。
機体と整備スタンドが揃ってから別途行う。

---

## 2. 共通の前提

### 2.1 着手前に揃っているべきもの

- [ ] 回路図どおりに配線した1輪ぶんの回路（E-3 まで）
- [ ] 安定化電源（電流制限＝CC が使えること）
- [ ] テスター
- [ ] `[env:bench]` と `[env:teleop]` がビルドできる環境（→ §0.2）
- [ ] 案B を反映した `motor_ledc.{hpp,cpp}`
- [ ] 部品: RRS（0.22Ω/1W以上）、C7〜C9 / C10〜C12（0.1µF）、JP1（2ピンヘッダ＋ジャンパ）

### 2.2 どの段でも守ること

⚠️ **電源を入れる前に、必ず次を確認する。**

1. **ESP32 をリセットしてから VM を入れる。** IN1/IN2 が振られている状態で主電源を入れると、
   入れた瞬間にモータが動く（アプリケーションノート 1.2 / 3章）。
   IC 内部に 100kΩ プルダウンがあるため、ESP32 がリセット中なら自動的に L に落ちる。
2. **USB を挿すなら JP1 を外す。** BP5293-50 の 5V と PC の USB 5V が並列になるのを防ぐ。
   ⚠️ 切るのは 5V の1本だけで、**GND は常に繋いだまま**にする。
3. **電流制限を先に設定してから出力を入れる。**

### 2.3 異常を感じたら

**発熱・異臭・異音・想定外の電流** のいずれかがあれば、**ただちに電源を切る**。
原因が分かるまで次へ進まない。log へ「中止」として残す。

---

## 3. E-0 — 無通電で線の色と系統を確かめる（タスク 8.2）

> ⚠️ **通電しない。** ここを誤ると次段で電源電圧がエンコーダへ入り、**エンコーダが即座に壊れる**。

### 3.1 なぜこの段が要るか

このモータは **黒＝エンコーダの GND、白＝モータ電源の−** である。
「黒＝共通グランド」と読んで 12V 系をエンコーダ対（青・黒）へ入れると、
エンコーダに電源電圧がそのまま掛かる。**一般的な配線の直感に反する**ため、
思い込みではなく計測器で確かめる。

### 3.2 手順

1. すべての電源を切り、バッテリを外す。
2. モータ6線のうち、**赤と白**の間の抵抗を測る。数Ω（モータ巻線）であること。
3. **青と黒**の間の抵抗を測る。赤白とは**導通していない**こと（別系統）。
4. 赤（または白）と、青・黒・黄・緑それぞれの間を測る。**すべて開放**であること。
5. 結果を [`bringup-log.md`](./bringup-log.md) の E-0 欄へ記録する。
6. [`wiring.md`](./wiring.md) §1 の表と突き合わせる。

### 3.3 合格条件

- [ ] 赤−白 が数Ωで導通する（モータ巻線）
- [ ] 青−黒 が赤白と**別系統**である
- [ ] モータ対とエンコーダ対の間に導通が**無い**
- [ ] [`wiring.md`](./wiring.md) §1 の記載と食い違いが無い

### 3.4 不合格だったら

ハーネスの色の割当がメーカー記載と違う可能性がある。**次へ進まない**。
実測した対応を [`wiring.md`](./wiring.md) へ反映してから、回路図の側も直す。

---

## 4. E-1 — 電流を制限できる電源で導通と短絡を確かめる（タスク 8.3）

> ⚠️ **バッテリを挿さない。** LiPo は短絡時に数十A を流せるため、配線ミスがあれば発煙する。

### 4.1 準備

- モータ・エンコーダ・ESP32 を**すべて外した**状態から始める。
- 安定化電源を **12.0V / 電流制限 0.2A** に設定する。

### 4.2 手順

各段で電流・発熱・異臭を確認しながら、**1つずつ**繋いでいく。

| # | 繋ぐもの | 電流制限 | 見るもの |
|---|---|---|---|
| 1 | 電源のみ（ドライバ・DC/DC 未接続） | 0.2A | 電流がほぼ 0 であること |
| 2 | ＋ BP5293-50（U4） | 0.2A | **TP2**（5V 出力）＝ 5.0V ±5% |
| 3 | ＋ ESP32（JP1 を挿す） | 0.5A | **TP3**（3V3 ピン）＝ 3.3V ±5%。消費電流が数十〜百数十 mA |
| 4 | ＋ 分圧回路（R1/R2/C6） | 0.5A | ⚠️ **TP4 が 3.3V 未満**（12.0V なら約 2.55V） |
| 5 | ＋ ドライバ U1（モータ未接続） | 0.5A | **TP1 / TP6** が入力電圧と同じ（差 0.2V 未満）。**TP7** が SGND 基準で約 0V |
| 6 | エンコーダ用コネクタ | 0.5A | ⚠️ **TP5（青−黒）＝ 3.3V。12V が出ていたら直ちに中止** |

⚠️ **TP4 と TP5 は、その先を繋ぐ前に測る。**
TP4 が 3.3V を超えていれば GPIO32 が壊れる。TP5 に 12V が出ていればエンコーダが壊れる。

測定点の一覧は [`schematic-draft.md`](./schematic-draft.md) §5.2 を参照。

### 4.3 合格条件

- [ ] 各段で電流制限に張り付かない（＝短絡していない）
- [ ] 発熱・異臭が無い
- [ ] TP1〜TP7 がすべて期待値の範囲内
- [ ] **TP4 < 3.3V**、**TP5 = 3.3V**

### 4.4 記録する項目

各段の消費電流、TP1〜TP7 の実測値、発熱・異臭の観察（要件 10.4）。

### 4.5 不合格だったら

電流制限に張り付いたら**その段で繋いだものが原因**である。切り分けは1つ戻すだけでよい。
これがこの段を1つずつ進める理由である。

---

## 5. E-2 — ドライバを介さずモータ単体を回す（タスク 8.4）

### 5.1 目的

回転方向とハーネス（赤/白）の対応を確定し、3個の無負荷電流のばらつきから
**個体差の外れ値をこの段で見つける**。ドライバが絡まないので、切り分けが最も簡単な段である。

### 5.2 手順

1. モータを回路から外し、**安定化電源へ直結**する（12.0V / 電流制限 2.5A）。
2. 赤を＋、白を−に繋ぎ、回転方向を記録する。**これを「正転」と定義する**。
3. 無負荷電流を記録する。
4. 赤と白を入れ替え、逆に回ることを確認する。
5. 3個すべてについて 2〜4 を行う。

### 5.3 合格条件

- [ ] 3個とも回る
- [ ] 赤白の入れ替えで回転方向が反転する
- [ ] 3個の無負荷電流に大きな外れ値が無い

### 5.4 記録する項目

各モータの回転方向と赤白の対応、無負荷電流3個（要件 10.5）。
⚠️ ここで決めた「正転」の定義が、以降すべての段の基準になる。

---

## 6. E-3 — 可変抵抗で開ループ確認（タスク 8.5）＝ **step 1**

### 6.1 この段で確かめること／確かめないこと

| | |
|---|---|
| 確かめる | ドライバ経由で、出力が**全域で滑らかに**変化すること。ドライバの発熱 |
| 確かめない | 回転方向の反転（BenchApp は正転しか出さない）、エンコーダ、無線、速度制御 |

⚠️ **エンコーダは繋がない。** BenchApp は計数器を一切初期化しない。
切り分けの対象を「ドライバとモータ」だけに保つのが、この段の設計意図である。

### 6.2 準備

- [ ] 案B を反映した `motor_ledc.{hpp,cpp}` でビルドすること
- [ ] 可変抵抗 10kΩ を **上端→3V3 / 下端→GND / ワイパ→GPIO33** の3本すべて結線する
      （2本だけだと中途半端な電圧になる）
- [ ] モータは**1個だけ**繋ぐ（BenchApp は3輪へ同じデューティを出すが、繋がっていない輪は無負荷）
- [ ] JP1 を**外す**（USB で書き込むため）
- [ ] 安定化電源 12.0V / 電流制限 **2.0A**

### 6.3 手順

1. `pio run -e bench -t upload` で書き込む。
2. ⚠️ **可変抵抗を絞りきった（0%）状態にする。**
3. **ESP32 をリセットしてから** 主電源を入れる。
4. **この時点でモータが動かないこと**を確認する。動いたら直ちに電源を切る（→ §6.5）。
5. 可変抵抗をゆっくり上げ、0% → 100% → 0% と往復させる。
6. 滑らかさ（引っかかり・段付き・うなりの有無）を観察する。
7. 数分回したあと、ドライバの発熱を触って確認する。

### 6.4 合格条件

- [ ] **絞りきりでモータが止まっている**（案Bが効いている決定的な証拠）
- [ ] 全域で滑らかに加減速する
- [ ] ドライバが触れないほど熱くならない
- [ ] 電流制限に張り付かない

### 6.5 ⚠️ 絞りきりでモータが回ったら

**案Bが効いていない。** 直ちに電源を切る。次のいずれかである。

1. 案Bを反映していないファームが書き込まれている
2. IN1 と IN2 の配線が入れ替わっている
3. ビルドが古い（`pio run -e bench -t clean` してから書き直す）

⚠️ **この症状は「前進・指令ゼロで全速逆転」であり、案B以前の状態そのものである。**
原因が分かるまで次へ進まない。

### 6.6 記録する項目

全域の滑らかさ、ドライバの発熱、消費電流の範囲（要件 10.6）。

---

## 7. E-4 — モータ非接続でコントローラの疎通（タスク 8.6）＝ **step 2 前半**

> ⚠️ **モータを接続しない状態で行う**（要件 10.7）。
> ペアリング・再接続・デッドマンの挙動が未検証のままモータが繋がっていると暴れる。

### 7.1 準備

- [ ] **モータを外す**（ドライバの OUT1/OUT2 から）
- [ ] 可変抵抗は外してよい（この段では使わない）
- [ ] `pio run -e teleop -t upload` で書き込む（bench ではない）

### 7.2 操作の割当

| 操作 | 割当 |
|---|---|
| デッドマン（出力許可） | **L1（左肩ボタン）** |
| 輪単体の選択 | 十字キー **← = 輪0 / ↓ = 輪1 / → = 輪2** |
| 選択中の輪の速度 | 左スティック 上下 |
| 機体走行（十字キーを押していないとき） | 左スティック＝並進、右スティック横＝回転 |

### 7.3 手順

1. DualSense をペアリングする。
2. ログで入力が届いていることを確認する。
3. **L1 を離した状態で指令がゼロになる**ことを確認する。
4. L1 を押しながらスティックを倒し、指令が出ることを確認する。
5. コントローラの電源を切り、**接続断で指令がゼロになる**ことを確認する。
6. 再接続できることを確認する。
7. 入力の遅れが実用に耐えるかを主観で記録する。

### 7.4 合格条件

- [ ] ペアリングできる
- [ ] 入力が届く
- [ ] **L1 を離すと指令がゼロになる**
- [ ] 接続断で指令がゼロになる
- [ ] 再接続できる

### 7.5 不合格だったら

⚠️ **接続方式そのものの成立可否の判断材料になる（OQ-16）。**
BT Classic 直結が使えないと判明したら、PC 経由（Serial / UDP）へ退避する。
その判断材料を log へ残す。

---

## 8. E-5 — コントローラで輪単体を回し、エンコーダを校正する（タスク 9.1 / M2a-0）＝ **step 2 後半**

> ⚠️⚠️ **この段から性質が変わる。** コントローラ経由の指令は**速度指令**であり、
> **速度PID を通る**。エンコーダが繋がって正しく数えていないと、測定速度がゼロのまま
> 指令だけが残り、**PID がデューティを上げ切る**。

### 8.1 準備

- [ ] E-1 の #6 で **TP5（青−黒）= 3.3V** を確認済みであること（12V が出ていたらエンコーダが死ぬ）
- [ ] エンコーダ4線を結線する。⚠️ **黒は ESP32 の GND ピンへ**（動力側の GND バスにぶら下げない）
- [ ] モータは**1個だけ**繋ぐ
- [ ] 車輪を浮かせる（机の縁など）

### 8.2 手順

1. **まずデッドマンを押さずに**電源を入れ、モータが動かないことを確認する。
2. 十字キー ← で輪0 を選び、L1 を押しながら左スティックをごくわずかに倒す。
3. **回り始めたら直ちにスティックを戻す。** 暴走しないことをここで確認する。
4. 問題なければ、ゆっくり出力を上げる。
5. 車輪を**手で決まった回数**（例: 10回転）回し、計数値を読む。
6. 分解能（パルス／出力軸1回転）を算出する。目安は 11 PPR × 減速比 約19 ≒ **209**。
7. 黄／緑のどちらが A相 かを、回転方向と計数の符号から確定する。

### 8.3 合格条件

- [ ] デッドマンを押さない状態でモータが動かない
- [ ] 指令に対して暴走しない（PID が発散しない）
- [ ] 計数値が回転に比例する
- [ ] 算出した分解能が 209 前後である
- [ ] A相/B相 と線色の対応が確定する

### 8.4 ⚠️ 暴走したら

**エンコーダが数えていない。** 直ちに L1 を離す（それで止まる）。次のいずれかである。

1. エンコーダの配線違い（黄緑の入れ替え、電源逆）
2. PCNT の設定ミス
3. プルアップ不足（→ [`schematic-draft.md`](./schematic-draft.md) §7.3 の予備位置を実装する）

### 8.5 記録する項目

分解能の実測値、A相/B相 と線色の対応、暴走の有無（要件 11.x、OQ の決着材料）。

---

## 9. E-6 — モータ3個を机上で回す ＝ **step 3**

> 🔶 **机上で車輪を浮かせて回すだけ**であり、整備スタンドは不要。
> 接地走行（逆運動学の符号確認）は本手順書の範囲外。

### 9.1 準備

- [ ] E-3 を3輪ぶん繰り返して済ませておく（1輪ずつ E-3 の合格条件を満たすこと）
- [ ] 3輪とも車輪を浮かせる
- [ ] ⚠️ **電流が3倍**になる。安定化電源の容量を確認する
      （1.5A 制限を入れていれば最大 4.5A、入れていなければ最大 6.9A）

### 9.2 手順

1. `[env:bench]` で、可変抵抗により**3輪を同時に**回す（BenchApp は3輪へ同じデューティを出す）。
2. 3輪の回り方・音・発熱を比べる。
3. `[env:teleop]` へ切り替え、十字キーで輪を切り替えながら1輪ずつ回す。
4. 十字キーを押さない状態（機体走行）で、左スティックを倒したときに
   **3輪が動くこと**だけを確認する。⚠️ **符号の正しさはここでは判断しない**（接地しないと分からない）。

### 9.3 合格条件

- [ ] 3輪とも回る
- [ ] 3輪の挙動に大きな差が無い
- [ ] 合計電流が想定内
- [ ] 十字キーで選んだ輪だけが回る

### 9.4 記録する項目

3輪の挙動の差、合計消費電流、発熱。

---

## 10. 記録先

[`bringup-log.md`](./bringup-log.md) へ追記する。⚠️ **上書きしない。**

各段について次を残す（要件 10.3, 10.4, 10.5, 10.6）。

- 実施日
- 書き込んだファームウェアの**コミットハッシュ**（どの版で確認したかが特定できないと記録の意味が無い）
- 使用した電源と設定（電圧・電流制限）
- 合格条件それぞれの合否
- 実測値
- 不合格だった場合、その内容と是正した内容
- 再実施したときは追記であり、上書きしない

---

## 11. この手順書が扱わないこと

| 事項 | 担当 |
|---|---|
| 接地走行と逆運動学の符号確認 | タスク 9.2（M2a-1）。整備スタンドと機体が要る |
| 速度制御のゲイン調整とオドメトリ | タスク 9.3（M2a-2） |
| 安全機能4種の発火試験 | タスク 9.4 |
| 回路図と実配線の食い違い | E-0 / E-1 で見るが、図そのものの照合は [`schematic-draft.md`](./schematic-draft.md) §8 の S-1〜S-13 |
| 未決事項の決着と文書の是正 | タスク 10.1 / 10.2 |
