# Implementation Plan

> **本 Spec は実機・ESP32 ペリフェラル・無線を一切必要としない。** すべてのタスクはホスト（WSL2）上で完結する。
> ⚠️ **数値パラメータの値を決めるタスクは1つも無い。** 本 Spec は型・不変条件・検証だけを持つ（要件 15）。
> 閾値・ゲイン・寸法・分解能の実測は `teleop-bringup` / `m2-motion-validation` が行う。

## 1. ファームウェアプロジェクトの骨組みと3ビルド構成

- [x] 1. ファームウェアプロジェクトの骨組みと3ビルド構成

- [x] 1.1 3つのビルド環境を定義し、純ロジックが3構成すべてからリンクされることを実証する
  - `firmware/platformio.ini` に `teleop` / `production` / `native` の3環境を定義する。ファーム2環境は classic ESP32 固定・ESP-IDF・C++17 を明示し、外部プラットフォーム定義は更新で内容が変化しないリリース成果物の URL として固定する
  - 純ロジックのディレクトリに、組込みビルド用のコンポーネント登録ファイルとホストビルド用のライブラリマニフェストを**同居**させる。組込み側のルート設定は、純ロジックのディレクトリを直接指す形にする（テスト専用ライブラリが探索対象へ入らないようにするため）
  - ファーム2環境ではホスト側の依存探索を無効化し、テスト専用ライブラリがファームウェアへ混入する経路を塞ぐ
  - アプリ層のエントリポイントと、そのビルド設定を最小構成で置く。この時点ではポートを結線しない
  - 純ロジック側に公開入口ヘッダと、リンク確認用の最小の実体を1つ置く
  - ホストテストと実機テストのディレクトリを分け、環境ごとに振り分ける設定を入れる
  - ⚠️ **「ホストテストが緑になること」だけを完了と扱わない。** 3構成のうち1つでも通らない骨組みは、以降のすべてのタスクの前提を壊す
  - 観測可能な完了状態: ホストテストが完走し、**かつ**テレオペ用・本番用の両ファームウェアがリンクまで到達することの3点すべてが揃う
  - _Requirements: 1.1, 1.2, 1.5, 1.6, 1.7, 16.7_

- [x] 1.2 テレオペ用ビルドと本番用ビルドを排他にし、本番から無線を排除する
  - アプリ層に、ビルド構成マクロが「ちょうど1つだけ定義されている」ことを検査するヘッダを置く。両方定義／どちらも未定義のときはコンパイル時に失敗させる
  - 各環境のビルドフラグで、対応するビルド構成マクロを1つだけ定義する
  - 本番用ビルドの設定で無線機能を無効化し、無線関連の外部依存を1つも持たせない。テレオペ専用ソースの置き場をビルド対象から除外する設定を入れる
  - **⚠️ ビルド構成マクロを純ロジック側で参照しない。** 純ロジックは3構成すべてで同一のソースであり、構成による分岐を持たない
  - 実機テストのレーンに、実機ツールチェーンでリンクが通ることだけを見る最小のテストを置く
  - 観測可能な完了状態: 両方のビルド構成マクロを同時に定義したビルドがコンパイル時に失敗すること、および本番用ビルドの生成された設定に無線無効化が反映されていることを、実行して確認できる
  - _Requirements: 1.3, 1.4, 16.7_

- [x] 1.3 ビルド構成を静的に検査する仕組みを、以降のタスクより先に用意する
  - 既存の Python テスト資産と同じ流儀で、`tests/` 配下に静的走査のテストを追加する（駆動制御ロジックの複製ではないため二重実装には当たらない）
  - **⚠️ この検査を後回しにしない。** 組込みビルド用のソース列挙とホスト側のソース実体の集合がずれると、**ホストテストだけが緑でファームウェアがリンクできない**状態が生まれる。タスク2以降はソースを追加し続けるため、**この検査が無い期間そのものが危険**である
  - 上記のソース集合の一致と、テスト専用ライブラリが組込みの探索対象に含まれていないことを検出する
  - 3つのビルド環境が存在すること、ホストテスト環境が実機を要さない構成であることを検出する
  - **テレオペ用と本番用のビルド構成マクロが互いに排他であること**を検出する
  - **本番用ビルドが無線関連の外部依存を持たず、無線無効化の設定を持つこと**を検出する
  - **ファーム2環境の対象基板が classic ESP32 に固定され、他系統の環境が定義されていないこと**を検出する
  - **外部プラットフォーム定義が更新で内容の変化しない成果物として固定されており、可変の参照になっていないこと**を検出する
  - ホストテストと実機テストの振り分け設定が存在することを検出する
  - 観測可能な完了状態: 上記すべての違反をわざと作り込んだ場合にテストが赤くなり、現状の構成では緑になることを確認できる
  - _Requirements: 1.3, 1.4, 1.6, 1.7, 16.3, 16.7_

## 2. 基盤層: 単位・型・設定・ポート

- [x] 2. 基盤層: 単位・型・設定・ポート

- [x] 2.1 単位換算の集約と設定エラーの型を定義する
  - 距離ミリメートル・時刻ミリ秒・電圧ミリボルトの換算係数と円周率を1箇所へ集約する。裸の数値定数を他のどのファイルにも書かない方針をここで確立する
  - 時刻から秒への換算、エンコーダ累積カウント差から距離への換算、角度の正規化を提供する。**カウントから距離への換算を行うのはこの1箇所だけ**にする
  - 設定検証の失敗を、例外を使わずに「原因の種別」「違反したパラメータ」「輪番号やテーブル位置の添字」の3点で返せる型を定義する
  - 観測可能な完了状態: 換算の往復（時刻・距離・角度の正規化）を検証するホストテストが緑になる
  - _Requirements: 4.2, 4.3, 7.7, 15.4, 15.6_

- [x] 2.2 指令・出力・状態・遮断理由のコア型を定義する
  - 時刻と経過時間、機体速度指令と輪ごとの目標速度指令、指令の受理結果、出力デューティ、電圧サンプル、累積カウント、位置姿勢、機体速度、遮断理由のビットマスク、制御ステップの結果を定義する
  - **整数を扱うすべての箇所で幅が処理系によって変化しない型を用いる。** 時刻は32ビットの折り返しを経過時間計算の考慮対象から外せる幅にする
  - 指令の型に、入力機器の状態・通信の詳細といった**指令元に固有の情報を一切含めない**
  - 遮断理由は、機体全体に効くものと輪ごとに効くものを同じビット定義で表現し、利用側が種別を判別できる形にする
  - 観測可能な完了状態: 各型のサイズと整数幅をコンパイル時表明で固定するホストテストが緑になり、ホストと組込みの両方でビルドが通る
  - _Requirements: 4.1, 4.2, 4.4, 5.1, 5.3, 14.4_

- [x] 2.3 (P) 全パラメータ構造体と設定検証を実装する
  - 機体寸法・エンコーダ分解能とホイール径・出力極性・運動上限・制御ゲイン・保護①〜④の閾値と継続時間・電圧換算テーブルを、それぞれ構造体として定義する
  - **⚠️ 数値パラメータに既定値を与えない。** 既定値を持つのは振る舞いを選ぶ真偽値・列挙（有効無効、保持か自動復帰か）のみとし、それが必須性能でも達成済み性能でもないことをコード上で明示する。実機の性能値を構造体の外にも内にも持たない
  - 検証で、有限性・正値性・定義域・順序関係（復帰閾値が停止閾値より高いこと等）・幾何の非退化・電圧テーブルの単調性と点数・移動平均窓の上限・極性が想定の2値であること・積分上限が有限であることを判定する
  - 動的メモリ確保を避けるための容量上限を定数として置く
  - 観測可能な完了状態: ゼロ初期化した設定が拒否されること、順序関係違反・退化した幾何・不正な極性がそれぞれ対応する原因種別で拒否されることを検証するホストテストが緑になる
  - _Requirements: 6.2, 7.6, 7.7, 9.2, 10.2, 11.6, 12.2, 13.3, 15.1, 15.2, 15.3, 15.4, 15.6_
  - _Boundary: DrivetrainConfig_
  - _Depends: 2.2_

- [x] 2.4 (P) 3つのペリフェラルポートを宣言する
  - エンコーダ読み出し・出力書き出し・電圧読み出しの3つを抽象として宣言し、**実装を1つも持たない**
  - 契約は物理量だけを往復させる。折り返しを含まない累積カウント、正規化されたデューティ、ミリボルトと妥当性フラグを扱い、**ペリフェラル固有の型・分解能・イベント形式を露出させない**
  - **無線・コントローラ・通信に関するポートを持たない**
  - 折り返しの桁上げと電圧換算は核が提供して実装側に利用させる契約であることを、宣言のコメントとして残す（判断・計算を実装側へ委ねないため）
  - 観測可能な完了状態: このヘッダが標準の固定幅整数ヘッダとコア型ヘッダ以外を参照せずにホスト・組込みの両方でコンパイルできる
  - _Requirements: 2.1, 2.3, 2.5, 2.6, 7.5, 17.5_
  - _Boundary: Ports_
  - _Depends: 2.2_

## 3. プリミティブと運動学

- [x] 3. プリミティブと運動学

- [x] 3.1 (P) ハードウェアカウンタの折り返しを跨ぐ累積器を実装する
  - 前回の生値との差を法で最短経路へ折り返して積算し、64ビットの累積値を返す。法はパラメータとし、ハードウェア側の折り返し方式が変わっても対応できるようにする
  - **サンプリング間隔中の真の変化量が法の半分未満であること**を事前条件として明記し、想定される最悪の回転速度と制御周期に対する余裕の見積もりをコメントとして残す
  - 観測可能な完了状態: 正転で折り返す列・逆転で折り返す列・正転と逆転を交互に跨ぐ列・累積値の表現範囲上限近傍を初期値にした列の4系統を検証するホストテストが緑になる
  - _Requirements: 4.4, 4.5, 7.1, 7.2, 7.3, 7.4_
  - _Boundary: WrapAccumulator_

- [x] 3.2 (P) 生の電圧読み値からバッテリ電圧への換算を実装する
  - 校正作業の出力（複数点で実測した「生値と実電圧の対」）をそのまま受け取れる区分線形テーブルとして換算する。点が2つだけのときは分圧比のみの線形換算へ自然に縮退させる
  - テーブル範囲外は両端の区間の傾きで外挿する
  - **分圧比や補正係数を実装内に固定値として持たない**
  - 観測可能な完了状態: テーブル点の上で厳密に一致すること、点間が線形であること、2点構成が分圧比換算と一致すること、範囲外が外挿されることを検証するホストテストが緑になる
  - _Requirements: 11.7, 15.6_
  - _Boundary: VoltageScaler_

- [x] 3.3 (P) 3輪オムニの逆運動学と順運動学を実装する
  - 各輪の取付角と機体中心からの距離をパラメータとして受け取り、設定時に**逆運動学の行列とその逆行列を1度だけ構築**する。三角関数の評価はこの1回に閉じ、制御ループ内では行列積のみを行う
  - **輪番号と符号の対応をここで定義し、実装内に重複させない**
  - 120度等配置を安全に組み立てる補助を用意し、三角関数の手書きを不要にする
  - 方向を保ったまま複数の値を等比縮小する共通処理を提供する（指令の輪速度にも出力デューティにも使う）
  - **上位の位置制御（目標座標から機体速度指令を求める処理）を持たない**
  - 観測可能な完了状態: 純並進・純回転・複合の各指令で逆運動学と順運動学の往復が許容差内で一致すること、純回転指令で3輪が同一符号・同一大きさになること、等比縮小の後も指令された運動の方向が保たれることを検証するホストテストが緑になる
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 8.1, 8.4_
  - _Boundary: Kinematics_

## 4. 速度制御とオドメトリ

- [x] 4. 速度制御とオドメトリ

- [x] 4.1 (P) 順運動学によるオドメトリを実装する
  - 各輪の周速から機体の並進2成分と回転を求め、**中点法**（機体座標系の変位を姿勢角の中点で回転させる）で世界座標系へ積分する
  - 姿勢角を正規化して保持し、位置は距離ミリメートル・時刻ミリ秒の単位で外へ出す
  - 原点と姿勢を外部からの操作で初期化できるようにし、**初期化時点からの経過時間と累積走行距離**をドリフト見積もりに使える形で提供する
  - **外部座標系への整合を自身の責務に含めない。** 原点は「最後に初期化した時点」以上の意味を持たない
  - 観測可能な完了状態: 直進・その場旋回・一定回転速度の円軌道が1周で閉じること、初期化後に原点へ戻ること、累積走行距離と経過時間が単調増加することを検証するホストテストが緑になる
  - _Requirements: 8.2, 8.3, 8.5, 8.6, 8.7_
  - _Boundary: Odometry_
  - _Depends: 3.3_

- [x] 4.2 (P) 各輪の速度PIDを2段プロトコルで実装する
  - **上限でクランプしない生の出力を返す算出**と、**実際に適用された値を受け取って積分を調整する確定**の2段に分ける。1段で飽和値を渡す形にすると、3輪まとめての等比縮小と個々のワインドアップ対策が噛み合わない
  - 比例・積分は偏差に、**微分は計測値の変化**に掛ける（目標値変更時のキックを避けるため）
  - 積分は絶対値上限でクランプし、加えて確定の段で「実現されなかった分」を積算し直さないようにする
  - 出力が遮断されている間に呼ぶ経路を用意し、積分をゼロに保ちつつ微分の基準を現在の計測値へ追従させる
  - **経過時間を外部から与えられた時刻から求め、内部で時刻を取得しない**
  - 観測可能な完了状態: 飽和が続く入力で積分が上限を超えないこと、算出値より小さい値で確定し続けても積分が育たないこと、遮断解除の直後の算出が過大な出力を返さないことを検証するホストテストが緑になる
  - _Requirements: 9.1, 9.2, 9.3, 9.4, 9.5, 9.6_
  - _Boundary: VelocityPid_

## 5. 保護①〜④と統合

- [x] 5. 保護①〜④と統合

- [x] 5.1 (P) 保護① モータロック検出を実装する
  - 出力指令が閾値以上かつ計測速度が閾値以下である状態の継続が、設定された継続時間を超えたときに発火する状態機械を実装する。**判定は輪ごとに独立**とし、他輪の状態を参照しない
  - 条件が崩れたら継続時間の計測をリセットし、継続時間に満たない一時的な速度低下では発火しないようにする
  - 復帰条件を自動復帰と手動リセットのどちらにも設定できるようにする
  - **⚠️ 判定入力は「出力上限を適用した後・遮断する前」の出力指令である。** 遮断後の値で判定すると発火した瞬間に条件が崩れて自己解除し、上限適用前の生の値で判定すると実際には出ていない指令を根拠に誤発火する
  - ハードウェアヒューズによる保護を前提にしない旨をコメントとして残す（1モータのストールでは溶断しないため、この保護がその領域の唯一の防護である）
  - 観測可能な完了状態: 継続時間を超えた拘束で発火し、満たない拘束では発火せず、輪ごとに独立して判定され、自動復帰と手動リセットの両設定が意図どおり動くことを検証するホストテストが緑になる
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5, 10.6, 10.7_
  - _Boundary: MotorLockDetector_

- [x] 5.2 (P) 保護② LiPo 低電圧保護を実装する
  - 電圧の読み値を**固定長の移動平均**（動的メモリ確保を行わない）で平滑し、警告閾値を下回った時点で警告状態を、停止閾値未満の継続が継続時間を超えた時点で停止状態を立てる
  - 復帰閾値を停止閾値より高く設定できるヒステリシスを持たせ、急加速による一時的な電圧降下では停止しないようにする
  - **読み値が得られないサンプルを移動平均へ取り込まない。** 欠測が継続時間を超えて続いた場合は、低電圧とは**別の理由**として停止する
  - 判定はすべてミリボルトの整数演算で行い、平均の丸めがプラットフォームに依存しないようにする
  - 観測可能な完了状態: 段階的な電圧低下で警告から停止へ遷移すること、一時的な降下では停止しないこと、復帰閾値まで戻って初めて復帰すること、欠測の継続が低電圧とは別の理由で停止させることを検証するホストテストが緑になる
  - _Requirements: 11.1, 11.2, 11.3, 11.4, 11.5, 11.8_
  - _Boundary: LowVoltageProtector_

- [x] 5.3 (P) 保護③ バッテリ電圧を考慮した出力上限を実装する
  - 計測されたバッテリ電圧に応じて出力上限を決定する。**状態を持たない純関数**として実装し、遮断理由のビットを持たせない
  - 上限の算出式を固定仕様として決め打ちせず、**外部から差し替えられる形**にし、かつ**無効化もできる**ようにする
  - 上限が設定された最大出力を超えないことを常に保証する
  - **読み値が得られない場合に上限を無制限とせず、外部から与えられた安全側の既定上限を適用する**
  - 観測可能な完了状態: 電圧に応じて上限が下がること、無効化すると常に最大出力を返すこと、差し替えた式の戻り値が定義域へ収まること、欠測時に安全側の既定上限が適用されることを検証するホストテストが緑になる
  - _Requirements: 12.1, 12.2, 12.3, 12.5, 12.6_
  - _Boundary: PwmCeiling_

- [x] 5.4 (P) 保護④ 指令ウォッチドッグを実装する
  - **最後に有効な指令を受けてからの経過時間だけ**を見て発火する。引数は時刻と「起動後に有効な指令を受けたか」の真偽値のみとし、**指令元に固有の情報を一切参照しない**
  - 判定は制御ステップ側から毎周期呼ばれる形にし、指令の到着イベントに依存させない
  - タイムアウト値をパラメータとして扱う
  - **⚠️ 本保護を物理的な非常停止手段の代替として位置付けない**旨をコメントとして残す。無線に依存する停止手段は運転操作であって安全装置ではない
  - 観測可能な完了状態: 指令を止めて時刻だけを進めるとタイムアウトで発火し、新しい有効指令で解除されることを検証するホストテストが緑になる。**指令の投入を一切行わずに時刻だけを進めても判定が働く**ことを含める
  - _Requirements: 13.1, 13.2, 13.3, 13.4, 13.7, 13.8_
  - _Boundary: CommandWatchdog_

- [x] 5.5 保護の合成・遮断値の決定・復帰操作を実装する
  - 保護①②④の検出器を保持し、**「検出器を1回進める」呼び出しと「直近の結果を合成する」呼び出しを別メソッドに分離**する。合成側は検出器を呼び直さない
  - **⚠️ 保護③（出力上限）はここに含めない。** 状態を持たない純関数であり、かつ上限値はPIDの算出直後（合成より前）に必要になるため、制御ステップ側が直接扱う
  - 合成では、設定未完了・出力未許可・指令未着・ウォッチドッグ・低電圧・電圧欠測を**機体全体の遮断理由**として、モータロックのみを**輪ごとの遮断理由**として立てる
  - いずれか一つでも理由が立っている輪の出力指令を遮断値にする処理を提供する。**物理的な遮断そのものは責務に含めず、ポートへ渡す値としてのみ表現する**
  - 保護ごとに保持するか自動的に解除するかを設定できるようにし、解除操作では**解除条件を満たす保護だけ**が解けるようにする
  - 観測可能な完了状態: 低電圧と1輪ロックを同時に成立させたときに機体全体の理由と輪ごとの理由が正しく分かれて立つこと、解除操作が条件の継続している保護を解かないこと、出力未許可と指令未着がそれぞれ別の理由として遮断値を生むことを検証するホストテストが緑になる
  - _Requirements: 14.1, 14.2, 14.3, 14.4, 14.5, 14.6, 14.7, 14.8, 14.9, 14.10_
  - _Depends: 5.1, 5.2, 5.4_

## 6. 指令入口と制御ステップの合成

- [x] 6. 指令入口と制御ステップの合成

- [x] 6.1 3つの指令入口を実装し、投入時点で輪目標速度へ正規化する
  - 機体速度指令は、運動上限でクランプしたうえで**投入時点で逆運動学を通し**、輪速度上限を超える場合は方向を保ったまま全輪を等比縮小して保持する
  - 輪ごとの目標速度指令は逆運動学を経由せず、各輪を個別に飽和させて保持する（1輪だけ回す用途には保存すべき運動方向が存在しないため）
  - **どちらの入口も同じ表現へ落とし、以降の層が指令の形も出自も知らない状態にする。** これにより輪単体指令にも保護①〜④が同一に適用されることが構造として成立する
  - クランプまたは縮小が行われたことを外部へ提供する。過去時刻の指令は受理しない（ウォッチドッグを欺けないようにするため）
  - 直前の有効な指令とその有効時刻を保持する。**指令を受け取ったこと自体を出力許可の条件として扱わず**、出力許可を与える入口を指令とは別に設ける
  - 観測可能な完了状態: 機体速度指令から得た輪目標速度と同じ値を輪単体入口へ直接与えたとき、保持される内容が一致することを検証するホストテストが緑になる。範囲外指令のクランプ報告と、過去時刻指令の拒否を含める
  - _Requirements: 5.1, 5.2, 5.4, 5.5, 5.6, 5.7, 5.8, 5.9, 6.5, 17.6_
  - _Depends: 3.3_

- [x] 6.2 制御ステップの骨格（設定・時刻・計測）を実装する
  - 設定を検証してから内部状態を構築する経路を用意し、**検証に失敗した状態では制御を開始させない**。ポートが揃っていないことも設定エラーとして拒否する
  - **状態を変えるすべての公開入口が時刻を引数に取る形**にし、現在時刻を内部で取得しない。自らループを回さない
  - 与えられた時刻が前回以前のときは、**ポートも読まず状態も更新せず、前回の結果をそのまま返す**副作用の無い短絡にする
  - 経過時間を実際の時刻差から求め、制御周期を固定値として前提にしない
  - エンコーダ累積カウントの差と経過時間から各輪の計測速度を求め、オドメトリを更新する
  - 観測可能な完了状態: 未設定の状態で制御ステップを回すと専用の遮断理由が立って遮断値が書き出されること、過去時刻を与えても内部状態が変化しないことを検証するホストテストが緑になる
  - _Requirements: 3.1, 3.2, 3.4, 3.5, 3.6, 15.4_
  - _Boundary: DrivetrainController_
  - _Depends: 2.3, 2.4, 4.1_

- [x] 6.3 制御ステップの出力段（上限・PID・縮小・遮断・極性）を実装する
  - 電圧サンプルから低電圧判定を進め、**出力上限を決定してからPIDを算出**する。上限は輪ごとの個別クリップではなく**3輪まとめての等比縮小**として適用する（1輪だけ飽和した瞬間に指令された運動の方向が崩れるため）
  - PIDの算出と確定の2段呼び出しを**この1箇所へ閉じ込め**、他のコードパスから直接呼べないようにする（呼び忘れ・二重呼び出しを構造で防ぐため）
  - ウォッチドッグ発火中は目標速度をゼロとして扱い、PIDを遮断中の経路に置く（直前の指令を継続して実行しないため）
  - 上限適用後・遮断前の出力指令でモータロック判定を進め、保護を合成し、理由が立つ輪のデューティを遮断値にする
  - 出力極性パラメータの適用を**最後の1箇所だけ**で行い、ポートへ書き出す
  - 観測可能な完了状態: 上限が下がった状態でも全輪へ同一の上限が掛かって指令方向が保たれること、ウォッチドッグ発火中に直前指令が継続実行されないこと、遮断理由が立つ輪だけが遮断値になることを検証するホストテストが緑になる
  - _Requirements: 6.5, 12.4, 13.5, 13.6, 14.1, 14.2, 14.3_
  - _Boundary: DrivetrainController_
  - _Depends: 4.2, 5.3, 5.5_

- [x] 6.4 状態スナップショットと下流が使う計測量を実装する
  - 制御ステップをブロックせず、ポートを読まず、ロックも取らない読み出し専用のスナップショットを提供する。単一スレッド前提であり、別タスクから読む場合の同期は呼び出し側の責務であることを契約として明記する
  - スナップショットに、輪ごとの目標速度・計測速度・デューティ・累積カウント、出力上限、平滑後の電圧と妥当性、低電圧の状態、遮断理由、オドメトリの状態を含める
  - **実効的に適用されているパラメータを実行開始時に確認できる**読み出しを設ける
  - **実測の制御周期**と**指令反映遅れ**を提供する。指令反映遅れは、保持された指令の有効時刻が前回と変化した制御ステップで「現在時刻と有効時刻の差」として算出し、次に変化するまで値を保持する
  - **⚠️ 実測値から既存の運動モデルのパラメータへの翻訳そのものを持たない。** 単位と定義を揃えた量を外へ出すにとどめる
  - 観測可能な完了状態: 新しい指令が反映された最初の制御ステップで指令反映遅れが算出され、次の指令が反映されるまで保持されることを検証するホストテストが緑になる
  - _Requirements: 15.5, 17.1, 17.2, 17.7_
  - _Boundary: DrivetrainController_
  - _Depends: 6.3_

- [x] 6.5 公開入口を確定させる
  - 下流 Spec が参照する唯一のヘッダを確定し、型・ポート・設定・累積器・電圧換算・運動学・制御ステップの合成体を再エクスポートする
  - **内部構造（オドメトリ・PID・保護の各検出器・保護の合成・指令入口）を再エクスポートしない。** これらは合成体の内部であり、下流が直接組み立てる対象ではない
  - **上位の位置制御・翻訳処理・無線関連を公開面に一切含めない**
  - 後続 Spec がポートを実装するために必要な契約が、実装を伴わずに揃っていることを確認する
  - 観測可能な完了状態: この1つのヘッダだけを取り込んだホストテストが、設定・指令投入・制御ステップ・状態読み出しの一連を実行して緑になる
  - _Requirements: 6.7, 17.5_
  - _Depends: 6.4_

## 7. ホスト検証基盤と閉ループ検証

- [x] 7. ホスト検証基盤と閉ループ検証

- [x] 7.1 テスト専用の車輪応答モデルと偽ポートを用意する
  - デューティから車輪速度への1次遅れモデルと、ストール注入、法で折り返した生カウントの生成を持つモデルを実装する。むだ時間は持たせない
  - 3つのポートの偽実装を用意する。エンコーダ側はモデルと累積器を実際の使われ方で接続し、電圧側は時系列と欠測を注入できるようにする
  - **⚠️ 係数は1箇所へ集約し、「合否条件ではない仮値であり、実測値でもなく、ここで得た数値を実機性能の主張に使わない」ことをファイル先頭で宣言する**
  - **本体のコードに含めず、テスト側にのみ置く。** ファームウェアの成果物へリンクされる経路を持たせない
  - 観測可能な完了状態: 偽ポートを差した制御ステップが実行でき、モデルの仮値宣言が存在することを検査する回帰テストが緑になる
  - _Requirements: 16.2, 16.3, 16.4_
  - _Depends: 6.5_

- [x] 7.2 (P) 閉ループで保護の発火と速度追従を検証する
  - 一定の機体速度指令に対して定常偏差が縮む方向へ動くことを確認する。**収束時間や具体的なゲイン値を合否条件にしない**
  - 1輪を拘束して保護①が当該輪のみを遮断値にすること、継続時間に満たない拘束では発火しないことを確認する
  - 電圧を段階的に下げて保護②が警告から停止へ遷移すること、急加速を模した一時的な降下では停止しないこと、復帰閾値まで戻して初めて復帰することを確認する
  - 満充電相当の電圧で保護③の上限が下がり全輪へ同一に掛かること、電圧欠測時に安全側の既定上限が適用されることを確認する
  - 逆運動学で与えた機体速度どおりに車輪が回った条件で、順運動学が同じ機体速度を復元することを確認する
  - **⚠️ ここで得た数値を実機性能の主張として扱わない**旨をテスト側にも明記する
  - 観測可能な完了状態: 上記5系統の閉ループテストが緑になり、**保護が発火する条件をホスト上で再現して実際に発火することが確認できる**
  - _Requirements: 8.4, 9.7, 10.1, 10.4, 11.2, 11.5, 12.1, 12.4, 12.6, 16.1, 16.6, 16.8_
  - _Boundary: NativeTestSuite closed-loop_
  - _Depends: 7.1_

- [x] 7.3 (P) 結合の振る舞いと決定性を検証する
  - 機体速度指令から得た輪目標速度と同じ値を輪単体入口へ与えたとき、**以降の保護・PID・遮断の振る舞いが完全に一致する**ことを確認する（保護の同一適用を構造ではなく振る舞いで裏取りする）
  - 指令を止めて時刻だけを進め、タイムアウトで遮断値になること、新しい指令で解除されても**出力許可が無ければ出力が出ない**ことを確認する
  - 設定直後の指令未着状態と、指令はあるが出力未許可の状態が、それぞれ別の遮断理由で遮断値になることを確認する
  - **同一の時刻列・指令列・ポート応答列で2回回し、制御ステップの結果と状態スナップショットの全フィールドが一致すること**を確認する
  - 保護の検出器を1ステップにつき1回ずつ進めてから合成する呼び出し順序で、継続時間の判定が実際の経過時間と一致することを確認する。同一ステップ内で検出器を二重に進める誤用が継続時間を早く進めることを回帰として固定する
  - 指令反映遅れが、反映された最初の制御ステップで算出され、次の反映まで保持されることを確認する
  - 観測可能な完了状態: 上記6系統の結合テストが緑になり、逆運動学・順運動学・オドメトリ・速度PID・累積器・保護①〜④のすべてが実機を用いずにホスト上でテストされている状態になる
  - _Requirements: 3.3, 5.6, 5.9, 13.1, 13.4, 13.5, 13.6, 14.3, 14.4, 14.6, 14.7, 14.9, 14.10, 16.1, 16.5, 17.1_
  - _Boundary: NativeTestSuite integration_
  - _Depends: 7.1_

## 8. 純ロジックのソースを静的に検査する

- [x] 8. 純ロジックのソースを静的に検査する
  - ビルド構成の静的検査（タスク 1.3）と同じ場所へ、純ロジックのソース走査を追加する
  - **ペリフェラルおよび組込みフレームワークに固有のヘッダの参照が混入していないこと**を検出する
  - **現在時刻を取得する手段の参照が混入していないこと**を検出する
  - **幅が処理系依存となる整数型と倍精度浮動小数点の混入**を検出する
  - 例外・実行時型情報・動的メモリ確保・重量級の標準ライブラリの参照が無いことを検出する
  - **ビルド構成マクロの参照が純ロジックに無いこと**、および**テスト専用ライブラリへの参照が無いこと**を検出する
  - 各ファイルの参照が設計の依存方向の許可範囲に収まっていること、およびシミュレータのコードを参照していないことを検出する
  - 観測可能な完了状態: 上記すべての違反をわざと作り込んだ場合にテストが赤くなり、現状のコードでは緑になることを確認できる
  - _Requirements: 2.2, 2.4, 4.6, 16.3, 17.3, 17.4_
  - _Depends: 1.3, 6.5, 7.1_

## 9. 未決事項の決着とドキュメントの是正

- [x] 9. 未決事項の決着とドキュメントの是正

- [x] 9.1 決定を記録し、未決事項一覧を更新する
  - **⚠️ このタスクは実装がすべて着地し、コードの配置が実態として定まってから行う**（是正対象が実態と一致している必要があるため）
  - リポジトリのディレクトリ構成の決定を、理由（組込みビルドではホスト側の依存探索が効かないため2つのマニフェストを同居させた経緯を含む）とともに決定の記録へ追加する
  - テレオペ用と本番用ファームウェアの排他方法の決定を、理由とともに決定の記録へ追加する。**2.4GHz 共存に加えて、無線ライブラリが依存する非オープンソース部品の封じ込めという別の根拠がある**ことを併記する
  - 決着した2件の行を未決事項一覧から削除する。**ID は欠番にし、再利用しない**
  - 無線ライブラリが依存する非オープンソース部品のライセンス取り扱いを、**新規の未決事項として末尾に採番**して登録する。**「何をすれば決まるか」を必ず記載する**
  - **⚠️ 未決事項を未決事項一覧以外の場所へ複製しない**
  - 観測可能な完了状態: 未決事項一覧から決着2件が消え、末尾に新規1件が決め方つきで加わり、決定の記録に対応する2件が理由つきで存在する
  - _Requirements: 18.1, 18.2, 18.4, 18.5, 18.7_

- [x] 9.2 本文側の参照を外し、steering を実態へ是正する
  - 駆動系の設計文書から、決着した未決事項への参照を外し、決定の記録へのリンクへ置き換える
  - 駆動系の設計文書の実機確認事項へ、**手元のマイコンの型番を確認して部品表へ明記する**項目を追加する（無線の方式上、他系統の型番が使えないため）。**未決事項一覧へは書かない**（現物を見れば決まることであり、課題として成立しないため）
  - プロジェクト構造の steering 文書の「将来のコード配置（案・未確定）」を、実態を記述した配置へ書き換える。「現時点でディレクトリを作らない」の記述を削除し、ファイル一覧ではなくパターンを書く
  - roadmap の該当箇所から、決着した未決事項への言及を外す
  - 観測可能な完了状態: 決着した未決事項の識別子がリポジトリ全体で未決事項一覧にも本文にも残っていないことを確認でき、steering のコード配置が実際のツリーと一致している
  - _Requirements: 18.3, 18.6, 18.8_
  - _Depends: 9.1_

## 追加タスク（2026-08-24）: 開ループ指令入口

> **本 Spec の実装完了・マージ後に追加された。** 契機は `teleop-bringup` の要件生成であり、
> `docs/requirements.md` の M2a-1「接地・オープンループ（PWM 直接指令）で手動走行」が
> 既存2入口では実現できないこと（どちらも必ず速度PID を経由する）が判明したためである。
> 下流が出力ポートへ直接書く回避策は保護①〜④をすべて迂回するため採らない。
> 詳細は design.md「フローの決定事項」と research.md の該当 Decision を参照。
>
> ⚠️ **新規ファイルを作らない。既存4ファイルへの追記のみで閉じる。**
> **`protection/*` と `velocity_pid.*` は変更しない** —— ここに手を入れる必要が出たら
> 分岐位置を誤った証拠である（design.md「開ループ入口の追加で変更するファイル」）。

- [ ] 10. 開ループ指令入口を追加する

- [x] 10.1 開ループ指令の型と指令入口を追加する
  - 各輪のデューティと有効時刻からなる指令の型と、有効な制御経路を表す列挙を追加する
  - 保持中の指令に**どちらの経路の指令か**と**各輪のデューティ**を持たせ、**既存の指令と同一の入れ物へ落とす**。別のラッチ変数を設けない（過去時刻の拒否・「起動後まだ一度も有効指令が無い」の判定・途絶判定が、いずれも1つの入れ物を見ることで成立している構造を壊さないため）
  - 開ループ指令の入口は各輪を**デューティの定義域で個別に飽和**させ、飽和が起きたことを既存と同じ形で外部へ提供する。**ここでは電圧由来の出力上限を適用しない**（上限は毎ステップ変わる量であり、投入時点で焼き付けると電圧降下に追従しなくなるため）
  - 過去時刻の拒否は経路によらず有効時刻の単調性のみで判定する。**経路をまたぐ指令の入れ替わりも通常の指令更新と同じに扱う**（経路が変わったこと自体を受理・拒否の条件にしない）
  - 観測可能な完了状態: 定義域を超える目標出力が定義域へ制限され飽和が報告されること、開ループ指令の投入後に保持中の経路が開ループになること、経路をまたぐ指令が過去時刻でない限り受理されることを検証するホストテストが緑になる
  - _Requirements: 5.10, 5.12, 5.13_
  - _Boundary: Types, CommandInput_

- [x] 10.2 制御ステップを3経路へ分岐させる
  - 出力段のヘルパーを、**途絶発火中／速度PID 経由／開ループ**の3経路へ分岐させる。分岐は**このヘルパーの内部だけ**に置き、遮断・極性適用・書き出しの経路は一切変更しない（保護の同一適用を実装の注意深さではなく構造で成立させるため）
  - 開ループ経路では、指令された各輪のデューティを生の出力として扱い、**輪ごとの個別クリップではなく3輪まとめての等比縮小**で出力上限を適用する（上限が効いた瞬間に指令した方向が歪むと、逆運動学の符号確認そのものが成立しなくなるため）
  - 開ループ経路では速度PID の2段プロトコルを一切呼ばず、**毎ステップ全輪の PID を保持リセットの経路に置く**。これにより経路の切り替え時に積分項が持ち越されない。**「切り替えを検出して状態をクリアする」明示的な機構は設けない**（検出漏れが静かな不具合になる形を避けるため）
  - 途絶発火中の扱いは経路によらず既存のままとする。途絶判定は分岐の**外側**に留め、指令元固有の事情を判定条件へ持ち込まない
  - 有効な経路を状態スナップショットへ含めて外部へ提供する
  - 観測可能な完了状態: 開ループ指令で走行させた状態から1輪を拘束するとモータロック保護が発火し当該輪のみが遮断されること、出力許可を落とすと遮断値になること、状態スナップショットの経路が期待どおりであることを検証するホストテストが緑になる
  - _Requirements: 5.11, 5.14, 5.15_
  - _Boundary: DrivetrainController_
  - _Depends: 10.1_

- [x] 10.3 開ループ指令の型を公開面へ再エクスポートする
  - 下流が参照する唯一の公開ヘッダへ、開ループ指令の型と制御経路の列挙を再エクスポート対象として追加する
  - **非公開のままにするもの（保護4部品・合成器・PID・指令入口の実体）の区別を変えない**。追加するのは型2つと、既に公開されている制御器が持つ入口だけである
  - 観測可能な完了状態: 公開ヘッダ1本だけを読み込んだ翻訳単位から、開ループ指令を構築して投入し、状態スナップショットで経路を確認できることを検証するホストテストが緑になる
  - _Requirements: 5.10, 5.15_
  - _Boundary: PublicApi_
  - _Depends: 10.2_

- [ ] 10.4 経路の等価性・途絶判定・切替の安全性を検証する
  - **速度PID 経由で最終的に得られたデューティと同じ値を開ループ入口へ直接与えたとき、保護①〜④の発火・遮断理由・遮断値・極性適用が完全に一致する**ことを検証する（要件 5.9 に対して既存タスクが行ったのと同じ形の裏取り）
  - 開ループ指令を投入したのち指令を止めて時刻だけを進め、**速度PID 経由の場合と同一の時刻で遮断される**ことを検証する
  - 速度PID 経由で積分項を十分に育てた状態から開ループへ切り替え、再び速度PID 経由へ戻したとき、**戻った最初のステップの出力が積分項ゼロから始まる場合と一致する**ことを検証する。⚠️ この試験が無いと積分項の持ち越しは静かな不具合として残るため、省略しない
  - 電圧を下げて出力上限が効く状況を作り、開ループでも**3輪の比が保たれたまま縮小される**ことを検証する
  - 観測可能な完了状態: 上記4件を含むホストテストが緑になり、既存のホストテスト一式にも回帰が無い
  - _Requirements: 5.11, 5.12, 5.14, 5.15_
  - _Boundary: DrivetrainController, CommandInput_
  - _Depends: 10.3_

## Implementation Notes

- **タスク 1.1**: `pio test` はディレクトリ名が `test_` で始まるものだけをテストスイートとして認識する（`platformio/test/helpers.py` の `list_test_names` で確認済み）。design.md の File Structure Plan が示す `test/native/units/` `test/native/kinematics/` 等のディレクトリ名は**このままでは `pio test` に一切収集されない**。以降のタスク（2.x〜7.x）で `test/native/` `test/embedded/` 配下にディレクトリを作る際は、design.md の名称の先頭に `test_` を補う（例: `units/` → `test_units/`）。テストが「収集されず0件パスした」形の静かな失敗になり得るため要注意
- **タスク 1.1**: `firmware/CMakeLists.txt` で `EXTRA_COMPONENT_DIRS` を設定する際は `set(...)` ではなく `list(APPEND EXTRA_COMPONENT_DIRS ...)` を使うこと。PlatformIO の espidf ビルダーが `PROJECT_SRC_DIR`（`firmware/src`）を含む `-DEXTRA_COMPONENT_DIRS` を独自に注入しており、`set()` で上書きすると `Couldn't find the main target of the project!` で失敗する
- **タスク 1.1**: `firmware/test/embedded/` 配下の Unity テストは `extern "C" void app_main(void)` を定義する（`native` 側の素の `main()` とは異なる。FreeRTOS の起動コードが `app_main()` を直接呼ぶため）
- **タスク 1.2**: `CONFIG_ESP_WIFI_ENABLED` は classic ESP32 では実プロンプトを持たない Kconfig シンボル（`default y if SOC_WIFI_SUPPORTED` で無条件 `y`）であり、`sdkconfig.defaults*` へどう書いても無効。**本番ビルドから Wi-Fi を実際に除外するのは `firmware/CMakeLists.txt` の `COMPONENTS` allowlist**（`production` 環境限定で `list(APPEND COMPONENTS "src" "drivetrain_control" "__pio_env")`）であり、`sdkconfig.defaults.production` の `CONFIG_BT_ENABLED=n` は Bluetooth には効くが Wi-Fi には効かない別経路。`EXCLUDE_COMPONENTS` は `wpa_supplicant` 等の推移的 `PRIV_REQUIRES` により実際には除外できないことを実測済み（採用せず）
- **タスク 1.2**: `build_src_filter` は `framework = espidf` では PlatformIO のビルドスクリプトが無視する（未サポート）。テレオペ専用ソースの除外は代わりに、ビルドプロファイルの環境変数を `firmware/src/CMakeLists.txt` が読んで `src/teleop/*.cpp` の glob を出し入れする方式（`firmware/scripts/set_build_profile_cmake_env.py`）で実現している
- **タスク 1.2**: `production` の `COMPONENTS` allowlist は現在 `src` / `drivetrain_control` / `__pio_env`（PlatformIO 内部の必須ダミーコンポーネント）の3つのみ。**以降のタスクで `production` 側に実ペリフェラル（`esp_driver_pcnt` 等）を使うコードが増えたら、このリストへ明示的に追加する必要がある**（positive list のため、追加しないと該当コンポーネントがビルドから静かに落ちる）。`teleop` 環境はこの allowlist の対象外（デフォルトのコンポーネント自動探索のまま）
- **タスク 1.3**: `tests/firmware/test_firmware_boundaries.py` を追加。この環境の `uv` venv は既定（`--group dev` のみ）だと `numpy` を要する既存26ファイル（`sensing_foundation`/`flying_object_tracking`/`world_frame_calibration`）の収集に失敗する。**リポジトリ全体のリグレッションは `uv run --all-extras --group dev pytest tests -q` で回す**（`tests/firmware` 単体は `--all-extras` 不要）。これは本 Spec が持ち込んだ問題ではなく既存の環境構成に起因する
- **タスク 1.3**: design.md の FirmwareBoundaryCheck が挙げる残りの項目（禁止 include・禁止トークン・型トークン・依存方向・`test_support` 参照検出）はタスク8の範囲。タスク1.3はビルド構成（`platformio.ini`/`CMakeLists.txt`/`sdkconfig.defaults*`）のみを検査する
- **タスク 3.2**: `VoltageScaler::toMilliVolts` は `float`/`double` を一切使わず `int64_t` の整数演算のみで区分線形補間する（非等分割の内挿点は「四捨五入（0から遠い方へ）」）。テーブル点上での厳密一致を構造的に保証する
- **タスク 3.3**: `config.cpp`（タスク2.3）は `[-sinα, cosα, R]` 行と行列式の非退化判定を独自にインライン実装している。これは design.md の依存方向（L2 は L0-L1 のみに依存、L5 kinematics は L0-L2 に依存）により `config.cpp` が `Kinematics` を呼べないための**設計上やむを得ない重複**であり、タスク3.3の欠陥ではない。将来、両者が依存できる層に行列式計算を共有関数として括り出す余地はあるが、それは新規の設計変更であり本タスクの範囲外
- **タスク 4.1**: 円軌道の閉合テストで `omega` を `N*Δθ = 2π` ちょうどになるよう選ぶと、位置閉合の判定が中点法固有の精度を検証しない（等間隔角度の変位ベクトル和は前進オイラー法・中点法・後退オイラー法のいずれでも「1のN乗根の総和=0」の恒等式によりほぼゼロへ収束するため、積分方式の違いを判別できない）。円軌道テストで積分方式の回帰を検知したい場合は `N*Δθ` が厳密に2πにならない omega を選ぶか、周回途中の中間偏差にも表明を掛ける
- **タスク 4.2**: `VelocityPid::commit()` の条件付き積分（design.md「適用値が生の出力より小さく」）は**大きさ（絶対値）の比較**として実装する。符号付き比較 `applied < raw` では、`scaleToLimit()` が常に非負の一様係数で全輪を縮小する（`docs` の方針どおり符号を保つ）ため、`raw` が負の場合に `applied`（0へ近づいた負値）が数値上 `raw` より大きくなり、負方向の飽和でアンチワインドアップが機能しなくなる。`|applied| < |raw|` かつ `sign(偏差) == sign(raw)` の両条件で判定することで両方向対称に動作する。design.md 本文の文言（「小さく」）は符号ではなく大きさを意図している旨を明確化済み（コミット参照）
- **タスク 5.1**: `MotorLockDetector::reset()` は無条件（発火条件が現在も成立しているかを再判定しない）で実装する。design.md の署名が `now` のみを取り、電流の条件を渡さないため構造的に自己判定できない。「解除条件を満たす保護だけを解く」（要件14.6）は `ProtectionSupervisor::resetProtections()`（タスク5.5）の責務。**タスク5.5実装時は、輪の拘束条件が現在も成立しているときに `MotorLockDetector::reset()` を呼ばないことを必ず確認する**
- **タスク 5.1**: `tests/firmware/test_firmware_boundaries.py` の `_actual_lib_source_paths()` が非再帰的な `glob("*.cpp")` を使っており、サブディレクトリ（`src/protection/` 等）の `.cpp` を検出できていなかった。`rglob("*.cpp")` に修正済み。design.md の File Structure Plan は L7 以降のソースを `protection/` 等のサブディレクトリに置くと定めているため、この修正はタスク5.1が最初に踏んだだけで以降のどのタスクでも必要だった
- **タスク 5.2**: `LowVoltageProtector` の `Unavailable → Normal` 復帰には `Tripped → Normal` と異なり `latching` の制約が無い（design.md の状態遷移図で確認済み: `Tripped --> Normal` のみ「保持設定が自動解除」の条件を持つ）。`latching=true` でも有効な読み値が復帰し移動平均が復帰閾値以上になれば `kUnavailable` は自己解除する。タスク5.5でこの非対称性を前提に実装すること
- **タスク 5.5**: `MotorLockDetector::reset()`/`LowVoltageProtector::reset()` はどちらも無条件（自己判定できない）ため、`ProtectionSupervisor::resetProtections()` が「解除条件を満たす保護だけを解く」（要件14.6）というゲーティングを担う。`MotorLockDetector` は latching=true で Tripped に入ると条件の再評価自体を止め、かつ生条件を読むアクセサを持たないため、`ProtectionSupervisor::updateLock()` が同一の閾値判定を**独自に再計算**して `last_lock_condition_[]` にキャッシュしている。これは design.md でロック済みの `MotorLockDetector` インターフェース（4メソッドのみ）を拡張できないための回避策であり、**将来 `motor_lock.cpp` の判定式を変更する際は `supervisor.cpp` の重複箇所も同時に直す必要がある**（コンパイラは同期を強制しない）。将来の design.md 改訂で `MotorLockDetector` に読み取り専用アクセサ（例: `conditionActive()`）を追加しこの重複を解消する余地がある
- **タスク 5.5**: `resetProtections()` の安全性は、同一制御ステップ内で `updateLock()`/`updateLowVoltage()` が**先に**呼ばれ `last_lock_condition_[]`/`last_voltage_sample_valid_` が最新化されていることに依存する。この呼び出し順序の事前条件は現状コード上に明記されていない。**タスク6（DrivetrainController）で `resetProtections()` を呼ぶ際は、必ず同一ステップの `updateLock()`/`updateLowVoltage()`/`updateWatchdog()` より後に呼ぶこと**（`compose()` と同じ呼び出し順序の制約）
- **タスク 6.2**: `Kinematics`/`Odometry`/`CommandInput`/`ProtectionSupervisor`/`VelocityPid` はデフォルトコンストラクタを持たないため、`DrivetrainController`（デフォルト構築可能・`configure()` 成功後にこれらを構築）は `alignas(T) std::byte storage_[sizeof(T)]` へのプレースメント new で遅延構築する `LazySlot<T>` ヘルパーを使う。**`get()` は必ず `std::launder(reinterpret_cast<T*>(&storage_))` を経由すること**（`Odometry`/`CommandInput` は `const Kinematics&` 参照メンバを持つため、再構築後のアクセスに `std::launder` を欠くと `[basic.life]p8` 違反の未定義動作になる。`emplace()` 直後の placement-new が返すポインタ自体は例外でランダリング不要）。`storage_` は `unsigned char[]` ではなく `std::byte[]` を使う（`unsigned ` の字面がタスク8の固定幅整数トークン検査と衝突するため）
- **タスク 6.2**: `DrivetrainController` の copy/move は `= delete` する（参照メンバを持つ内部オブジェクトのメンバワイズコピーは参照先を誤らせるため）。`configure()` は non-null な3ポートの検証も行うが、null ポートに対応する専用の `ConfigField` 列挙子が無いため `ConfigError::kOutOfRange` ＋ `ConfigDiagnostic::index`（0=encoder/1=motor/2=battery）を暫定的に流用している。**タスク6.5で公開エラー診断面を確定する前に、専用の列挙子（例: `kPortsEncoderNull` 等）を追加するか検討すること**
- **タスク 6.3**: ウォッチドッグ発火中は `compute(0, measured, dt)` ではなく `holdReset()` が compute/commit ペアを**完全に置き換える**（`VelocityPid` の Precondition「holdReset() を呼んだステップでは compute/commit のどちらも呼ばない」と、design.md の「PID を holdReset に置く」という文言から）。`CommandWatchdog` は `has_command==false` のとき無条件に `tripped()==true` になるため、`kNoCommandYet` は常に `kCommandTimeout` と同時に立つ（設計上の帰結であり不具合ではない）
- **タスク 8**: 静的走査の禁止トークン検査は、既にコミット済みのコードが禁止トークンについて説明するコメント（例:「`unsigned char` を避けて `std::byte` を使う理由」）を持つため、コメントを除去してから走査しないと**規約を最も丁寧に守っているファイルほど誤検知される**。全トークン系チェックの前に `strip_cpp_comments()`（文字列/文字リテラルは保持）を適用すること。`new` の禁止判定は `new T(...)`（禁止）と `new (ptr) T(...)`（placement new・許可）と `#include <new>`（許可）を区別する必要があり、`(?<!<)\bnew\b(?!\s*\()` で対応した
- **タスク 6.4**: `ProtectionSupervisor` に `averagedBatteryMilliVolts()`/`batteryVoltageValid()`（`DrivetrainStatus.battery_milli_volts`/`battery_valid` を埋めるための読み取り専用転送）を追加し、design.md の Service Interface へも同期済み。**既に承認済みタスクのインターフェースを拡張する場合は、実装と同時に design.md も更新すること**（黙って拡張してレビュー任せにしない）。また `uv run --all-extras --group dev pytest` は WSL ブリッジ経由でのみ到達可能（Windows ネイティブの Bash/PowerShell には `uv` が無い）。ネイティブ側で `uv` が見つからないことを理由に回帰検証を省略しない
- **タスク 6.5**（タスク5.5の警告を解消）: `DrivetrainController::resetProtections()` を公開し `ProtectionSupervisor::resetProtections()` へ委譲した。タスク5.5が指摘した「`resetProtections()` は直近の `step()` が `updateLock()`/`updateLowVoltage()`/`updateWatchdog()` を先に呼んでいることに依存する」という事前条件を、`controller.hpp` のドキュメントコメントと design.md の `DrivetrainController` Preconditions の両方へ明記した。**`resetProtections()` は必ず直近の `step()` 呼び出しの直後に呼ぶこと**（別スレッドの非同期コマンド処理等から間隔を空けて呼ばない）。この制約は再エクスポートされる公開ヘッダ（`drivetrain_control.hpp`）からも読める唯一の契約面（`controller.hpp` 自身のコメント）に置いてあるため、下流 Spec（`teleop-bringup` 等）はこれを読める
- **タスク 2.1**: `pio run -e teleop` の並行実行（フォアグラウンドとバックグラウンドを同時に走らせる等）は同一 `.pio/build/teleop` への同時書き込みでファイル破損を招く（`objdump: file format not recognized` 等の紛らわしいエラーになる）。**PlatformIO のビルドは常に1つずつ・フォアグラウンドで実行する**
- **feature検証（タスク6.2/6.5の未決事項の決着）**: `sdkconfig.defaults.teleop` は作らないことを design.md の File Structure Plan に明記した（`platformio.ini` の `[env:teleop]` は実際には `SDKCONFIG_DEFAULTS` を上書きせず `sdkconfig.defaults` 単独を使っており、無線有効化は `teleop-bringup` の責務のため本 Spec では未着手のままでよい）。また null ポート設定エラー用の専用 `ConfigField` 列挙子（`kPortsEncoderNull` 等）は追加しないと最終判断した（`ConfigError::kOutOfRange` ＋ `ConfigDiagnostic::index` の暫定表現のまま。機能上の実害がなく、具体的な下流ニーズが無い状態での speculative なインターフェース拡張を避けるという本実装全体の方針に合わせた）。`controller.hpp` のコメントも「レビュー時に要判断」から「現状維持と最終判断」へ更新済み
