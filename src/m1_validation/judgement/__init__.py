"""判断を担うサブパッケージ（design.md「File Structure Plan」/「L7: 判断」）。

本 Spec が出す「判断」——OQ-27（Pi 4 継続可否）・OQ-05 の判断材料・時間予算表
の更新値——を、問いごとのモジュールへ分ける。現時点では `oq27.py`（OQ-27 の
判定）が存在する。

判断は例外なく `types.Judgement` の共通の形に載る。**実測前に固定した規則の
説明文を判定値と同じ場所に置く**ためであり、規則が結果から離れた場所にあると
あとから規則のほうを結果に合わせて読み替えられてしまう（要件 9.1）。

本ファイル自体はサブパッケージのマーカーであり、ロジックを持たない。
公開 API の再エクスポートは行わない——下流が参照してよい入口は
`m1_validation/__init__.py` ただ1つである（同ファイルの docstring 参照）。
"""

from __future__ import annotations

__all__: list[str] = []
