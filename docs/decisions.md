# 決定と不採用案の記録（Decision Log）

要件: [requirements.md](./requirements.md) / 駆動系: [drivetrain-spec.md](./drivetrain-spec.md) /
部品表: [bom.md](./bom.md) / 開発環境: [development-environment.md](./development-environment.md) /
独自機能: [original-features.md](./original-features.md) / 未決事項: [open-questions.md](./open-questions.md)

> **このファイルの役割**
> 1. **方針転換の記録** — 何を、いつ、なぜ変えたか（§1）
> 2. **採用しなかった案** — なぜ採らなかったか（§2〜§4）
>
> 各仕様書は「**現在どうなっているか**」だけを書き、**「昔どうだったか」はここに置く。**
> 以前は各ファイルの冒頭に版差分ブロックが積み上がり、巻末に旧案アーカイブが付いていたため、
> 本文を読む前に履歴を読まされる状態になっていた。
>
> ⚠️ **不採用案を消さない理由**: 「なぜ BTS7960 ではなく TB67H450 なのか」「なぜ既製ベースを買わないのか」は
> 数ヶ月後に必ず再燃する。理由が残っていないと、同じ検討をやり直した上で**間違った結論に至る**。

---

## 1. 方針転換の記録

### D-1 最高速度を合否条件から取り下げた

| | |
|---|---|
| **変更前** | 「最高速度 ≥ 1.5 m/s」を絶対的なハード要件とし、これを満たすことを合否条件にしていた |
| **変更後** | 評価軸を**短時間移動性能**（0.4 / 0.6 / 0.9 秒で何 m 動けるか）へ変更 |
| **理由** | 本プロジェクトの目的は指定速度の達成ではなく、**「落下地点を予測し、残された時間内にキャッチ可能範囲へ到達すること」**。最高速度は手段の一指標にすぎず、加速性能を無視した数値だけを合否にすると誤った最適化を招く |
| **現在の扱い** | 1.5 m/s は**参考目標値**。→ [NFR-1](./requirements.md#nfr-1-短時間移動性能-主評価軸) |

あわせて、旧設計目標値を以下の扱いに変更した。**いずれも合否条件ではない。**

| 項目 | 旧目標値 | 現在の扱い |
|---|---|---|
| 最高速度 | ≥ 1.5 m/s（設計最高 ~1.8 m/s） | **参考目標値** |
| 加速度 | ~2〜3 m/s² | **参考値**。必須条件ではない（→ [NFR-2](./requirements.md#nfr-2-加速安定性)） |
| 車体質量 | ~3〜4 kg | **参考値**。上限を設定しない。設計方針としては 2〜2.5 kg 程度が望ましい |

### D-2 未実測の数値を合否条件にしない規律を全 NFR へ拡大

NFR-1 / NFR-2 のみに適用されていた規律を **NFR-3 / NFR-4 / NFR-5 にも適用**し、
すべて暫定目標値として導出根拠を併記する形に統一した。

- **NFR-3**: 「< 250 ms」→ **≤ 200 ms**（時間予算表の区間2＋区間3 と整合しない矛盾を解消）
- **NFR-4**: 「≥ 60 fps 相当」という根拠のない固定値を撤回し、
  **「必要サンプル数 ÷ 許容時間」から必要 fps を導出する**形へ。また固定側の検出 fps と
  移動体側の制御周期が1項目に混在していたため、**両者を分離**した
- **NFR-5**: 「< 開口半径」→ **< 開口半径 − ゴミの代表寸法/2**
  （旧定義はゴミを大きさのない点として扱っていた。実際は縁にかかった時点で弾かれる）

### D-3 安全と成立性の要件を追加

**「無いと系として成立しない」ものであり、演出や追加機能ではない**という位置付けで以下を追加。

| 要件 | 追加理由 |
|---|---|
| **FR-10** 通信途絶フェイルセーフ | 移動体は「最後に受け取った目標座標」へ走り続ける実装になりやすい。FR-9（ロック・低電圧）では検出できない |
| **FR-11** 動作領域制限 | 誤検出・予測発散により床面外や遠方の座標が目標として送られ得る |
| **FR-12** キャッチ保持 | 「開口に入れた」と「キャッチできた」は別問題。NFR-7 の成功判定に直結 |
| **NFR-6** 到達時の静定 | 「落下時刻に目標点を通過する」のと「目標点で待っている」のは別物 |
| **NFR-7** キャッチ成功率 | **v0.2 まではプロジェクト全体の完了条件が存在しなかった。** 部分指標を全部満たしてもキャッチできるとは限らない |

### D-4 手動テレオペの追加と M2 の分割

| | |
|---|---|
| **問題** | M2 の定量計測は「逆運動学が正しい」「モータの回転方向とホイール割当が合っている」「エンコーダの符号が合っている」ことを**暗黙の前提**にしていたが、**その前提を検証する段階が存在しなかった** |
| **決定** | [FR-13 手動テレオペ](./requirements.md#2-機能要件fr)（開発用）を追加し、M2 を **M2a（手動ブリングアップ）/ M2b（自動計測）**に分割 |
| **副次的決定** | **指令ウォッチドッグを M2a より前に実装**し、FR-10 はその**入力元差し替え**として実現する。テレオペならコントローラの電源を切るだけで発火試験ができる |
| **境界** | **NFR-1 の合否は M2b でのみ判定する**（人間の入力には再現性がないため）。テレオペは**物理的な非常停止手段の代替にならない** |

### D-5 独自機能のスコープ絞り込み

| 対象 | 判断 | 理由 |
|---|---|---|
| 柱1 軌道シミュレータ | **採用** | ハード完成前に NFR-1 の成立性を評価できる唯一の手段 |
| 柱2a 構造化ロギング | **採用** | 独自機能というより前提条件。これが無いと M1・M2 が完了しない |
| 柱2b ライブダッシュボード | **保留** | M1・M2 で見たい現象はリアルタイム観測には速すぎる（→ [OQ-38](./open-questions.md#g-観測基盤独自機能)） |
| 柱3 投擲アーカイブ／ベンチマーク | **見送り** | 蓄積データが無い段階では価値が出ない（→ [OQ-39](./open-questions.md#g-観測基盤独自機能)） |
| 柱4 手動テレオペ | **採用** | 組み立て直後のブリングアップ手段が定義されていなかった（D-4） |
| 7.1 待機位置の学習 | **見送り** | 柱3 が前提のため自動的に見送り |

**Hono を今は使わない**という判断もここに含まれる。Python が JSON を出力しブラウザが描画するだけの構成では
常駐サーバが不要であり、HTTP API を持つ用途（柱2b）を保留した時点で**使う理由が消えた**。
> 使いたい技術に合わせて用途を作らない。柱2b を作ると判断した時点で改めて検討する。

### D-6 BOM の購入ステータス管理を廃止

部品調達がおおむね完了したため、✅/🛒/⬜/❓ による進捗管理をやめ、
**「確定した構成部品表」**へ性格を変更した。機種・数量が未確定のものは購入状態としてではなく、
**設計課題として [open-questions.md](./open-questions.md) へ**集約する。

### D-7 ドキュメントの再編

未確定事項が5ファイル・7リストに分散し、主要項目が3〜4箇所に重複していた
（メインスイッチ4箇所、通信方式4箇所、非常停止4箇所など）。
**1つの決定で3〜4ファイルを編集する必要があり、直し忘れが「決まっているのに未確定と書いてある箇所」を生む**状態だった。

| 施策 | 内容 |
|---|---|
| 未決事項の一元化 | [open-questions.md](./open-questions.md) を単一の正とし、本文には `→ OQ-nn` の参照のみ置く |
| 履歴の分離 | 版差分ブロックと旧案アーカイブを本ファイルへ集約。**各仕様書は「現在どうなっているか」だけを書く** |
| 部品情報の一元化 | **型番・数量は [bom.md](./bom.md) を正**とし、他ドキュメントは参照に留める |

> **削らなかったもの**: 各数値の導出根拠（10A ヒューズの根拠、時間予算表の内訳、NFR-5 の算出式など）と
> ⚠️ 注記群。これらは「数値だけが残って根拠が消える」最悪の状態を防ぐための本体であり、
> **本プロジェクトのドキュメントで最も価値がある部分**と判断した。

### D-8 Throw Record 最小スキーマを prediction-core で確定した（OQ-31 決着）

| | |
|---|---|
| **決定** | Throw Record（1投擲＝1レコード）の最小スキーマを `prediction-core` の実装（[design.md](../.kiro/specs/prediction-core/design.md) ThrowRecordCodec）で確定した。フィールドは `schema_version`（現行 `1.0`）・`record_id`・`source`（`live`/`recorded`/`simulated`）・`config`（予測に使用したパラメータ）・`samples`（観測サンプル系列）・`predictions`（予測結果系列。要素に `kind` キーを持たせ `Prediction`/`InvalidPrediction` の直和型を判別する）・`extra`（下流の加算的拡張の退避先）の7つ |
| **理由** | シミュレータ（柱1）・M1実測評価・`sensing-foundation` の記録形式（OQ-32）がいずれも「1投擲」という同じ単位を扱うにもかかわらず、スキーマを別々に定義すると記録・再生・評価が入力元（live/recorded/simulated）によって食い違う。`prediction_core` を単一定義元とし、下流はこれに従う方針にした（→ [requirements.md Requirement 9](../.kiro/specs/prediction-core/requirements.md)） |
| **柱3見送り（D-5）との関係** | 当初 OQ-31 は「柱3（投擲アーカイブ）を見送ったため最初から完全な形にしない」という条件付きだった。実装時点でも柱3向けの集計・検索機能は持たせておらず、`to_dict`/`from_dict`/`to_json`/`from_json`/`replay`/`predictions_equivalent` という素の構造・直列化・再生のみに留めている（方針を変更していない） |
| **現在の扱い** | `prediction_core.ThrowRecord` として実装・テスト済み（`prediction-core` spec 全19タスク完了、263件のテストで検証）。下流 Spec（`sensing-foundation` の Record/Replay 形式＝[OQ-32](./open-questions.md#g-観測基盤独自機能)、テレオペログ＝[OQ-37](./open-questions.md#g-観測基盤独自機能)、ログ保存形式＝[OQ-35](./open-questions.md#g-観測基盤独自機能)）はこのスキーマに従う。**保存先・拡張子・NDJSON化はスキーマの範囲外**であり、これらは引き続き `sensing-foundation` 側の未決事項として残る |

### D-9 シミュレータの物理モデルの詳細度を最小限で決着した（OQ-33 決着）

| | |
|---|---|
| **決定** | `trajectory-simulator`（柱1）の初期物理モデルに含める要因・含めない要因を、投擲物理・観測・移動体・キャッチ判定の4段について確定した。含めるのは「投擲物理: 空気抵抗を無視した放物運動＋投擲条件のばらつき」「観測: 検出開始遅れ・サンプリング周期・位置ノイズ・欠測」「移動体: 加速度上限・最高速度・減速による性能上限の近似」の3点のみ。含めないのは「投擲物理: 空気抵抗・回転（マグヌス効果）・跳ね返り」「観測: センサ固有の歪み・視野外判定・遮蔽」「移動体: ホイールスリップ・方向依存の性能差・逆運動学・PID」「キャッチ: 跳ね返り（bounce_out）」 |
| **理由** | [open-questions.md](./open-questions.md) OQ-33 が「最初は最小限とし、M1・M2 の実測で不足が分かってから足す」としていた方針をそのまま実装に落とし込んだ。要因を含めない場合は**その旨を出力に明示する**ことで、「入れていないものが入っているように見える」誤った安心を防ぐ（`trajectory-simulator` requirements.md 要件1.5 / 2.8 / 4.6 / 5.7 / 9.7） |
| **現在の扱い** | `trajectory_sim.MODEL_EXCLUSIONS`（`src/trajectory_sim/results.py`）として実装・テスト済み。掃引結果の出力 JSON には `model_exclusions` キーとして必ず含まれ、較正段階・パラメータ出所と並んで「この結果がどこまで信用できるか」を読み手に伝える必須項目になっている。追加が必要になる契機は M1・M2 の実測であり、その時点で改めて `MODEL_EXCLUSIONS` を見直す（要因の増減は `trajectory-simulator` design.md の Revalidation Trigger に該当する） |

### D-10 リポジトリのディレクトリ構成を確定した（OQ-40 決着）

| | |
|---|---|
| **決定** | 移動体側のコードは `firmware/` 以下に置く。`firmware/lib/drivetrain_control/` に純ロジック（`include/` + `src/`）を置き、**`CMakeLists.txt`（`idf_component_register`）と `library.json`（PlatformIO ライブラリマニフェスト）を同一ディレクトリに同居させる**。`firmware/lib/test_support/` はホストテスト専用のフェイクポート・プラントモデルを置くが、**`library.json` のみを持ち `CMakeLists.txt` を意図的に持たない**。ルート `firmware/CMakeLists.txt` は `list(APPEND EXTRA_COMPONENT_DIRS "lib/drivetrain_control")` で**このディレクトリのみを直接指す**（`lib/` 全体は指さない）。アプリ層は `firmware/src/`（`build_profile.hpp`・`main.cpp`・`CMakeLists.txt`）、テストは `firmware/test/native/`（ホスト、`test_` 接頭辞必須）と `firmware/test/embedded/` に分離する |
| **理由** | `framework = espidf` では PlatformIO 自身のライブラリ依存探索（LDF）が IDF コンポーネントを発見できない（PlatformIO は `native` 環境でのみ `library.json` を見る）。一方 IDF 側のビルドは `EXTRA_COMPONENT_DIRS` に列挙されたディレクトリしかコンポーネントとして発見しない。**同一の純ロジックを2つのビルドシステムのどちらからも発見させる**必要があり、かつ `tech.md` 開発標準3（二重実装の禁止）によりソースを2箇所へコピーする案は採れない。そこで2つのマニフェストを同一ディレクトリへ同居させ、`native` からは `library.json` として、ファーム2環境（`teleop`/`production`）からは `EXTRA_COMPONENT_DIRS` 経由の `CMakeLists.txt` として、それぞれ発見させる方式にした（詳細な比較検討は [research.md](../.kiro/specs/drivetrain-core/research.md) "Decision: 純ロジックを「2マニフェスト同居ディレクトリ」として配置する" を参照）。`EXTRA_COMPONENT_DIRS` が `lib/` 全体ではなく `lib/drivetrain_control` を**直接**指すのは、`lib/test_support/`（`CMakeLists.txt` を持たない）がファームウェアへ混入する経路を構造的に消すため |
| **現在の扱い** | 実装・テスト済み（`drivetrain-core` spec 全27タスク完了）。`firmware/CMakeLists.txt` / `firmware/lib/drivetrain_control/{CMakeLists.txt,library.json}` / `firmware/lib/test_support/library.json` として存在し、`teleop`・`production`・`native` の3環境すべてでビルド・テストが通ることを確認済み |

### D-11 テレオペ用と本番用ファームウェアの排他方法を確定した（OQ-21 決着）

| | |
|---|---|
| **決定** | ビルドプロファイルはコンパイル時マクロ `DRIVETRAIN_BUILD_TELEOP` / `DRIVETRAIN_BUILD_PRODUCTION`（`platformio.ini` の `[env:teleop]`/`[env:production]` `build_flags`）で表現する。`firmware/src/build_profile.hpp` が `#error` により「どちらか一方のみが定義されていること」を強制する。テレオペ専用ソース（`src/teleop/*.cpp`）は `firmware/src/CMakeLists.txt` が同名のビルドプロファイル**環境変数**（`firmware/scripts/set_build_profile_cmake_env.py` が設定）を読んで glob を出し入れすることで除外する。加えて `production` 環境限定で `firmware/CMakeLists.txt` に `COMPONENTS` allowlist（`src`/`drivetrain_control`/`__pio_env` の3つのみ）を適用し、`sdkconfig.defaults.production` で `CONFIG_BT_ENABLED=n` を設定する |
| **理由** | 独立した2つの根拠がある。**(1) 2.4GHz 共存**: テレオペの Bluetooth（BT Classic／Bluepad32・DualSense）と本番想定の無線が同一 2.4GHz 帯を使うため、同一ファームに同居させると干渉・リソース競合の懸念がある（[drivetrain-spec.md §10.1.2](../drivetrain-spec.md#1012-テレオペ用ファームウェアは分離する)）。**(2) 非オープンソース部品の封じ込め**: Bluepad32 本体は Apache-2.0 だが依存する BTstack は「個人的利益のためのみ。商用目的・金銭的利得のためには使用不可」という条項付きでオープンソースではない。**テレオペビルドにのみ**この encumbrance を封じ込めることで、本番ファームウェアの成果物をライセンスクリーンに保てる（GitHub 公開時にトップレベル LICENSE が BT 版ファームまで自由に再利用可能だと誤読させないため。→ [OQ-42](./open-questions.md#g-観測基盤独自機能)）。実装時に判明した点として、`CONFIG_BT_ENABLED=n` は Bluetooth には効くが、classic ESP32 では `CONFIG_ESP_WIFI_ENABLED` が実プロンプトを持たない Kconfig シンボル（`default y if SOC_WIFI_SUPPORTED`）であるため `sdkconfig.defaults*` への設定は Wi-Fi に対して無効であり、また `EXCLUDE_COMPONENTS` も `wpa_supplicant` 等の推移的 `PRIV_REQUIRES` により無線コンポーネントを再度引き込むため機能しない（いずれも実測で確認）。**実際に本番ビルドから無線を除外しているのは `COMPONENTS` allowlist**である |
| **現在の扱い** | 実装・検証済み。`production` 環境の `firmware.map` に `libesp_wifi.a`/`libbt.a`/`libwpa_supplicant.a`/`libesp_phy.a` への参照がゼロであることを確認済み（flash image は 161985 → 135557 バイトへ縮小）。BTstack ライセンスの取り扱い自体は本決定の対象外であり、新規の未決事項として → [OQ-42](./open-questions.md#g-観測基盤独自機能) へ登録した |

---

## 2. 採用しなかった案: 駆動系

> 以下は drivetrain-spec v0.1 の内容。**いずれも採用せず**、経緯の記録として残す。

### 旧案: 100mm ホイール構成（採用せず）

- ホイール: Nexus **14049** 100mm ダブルオムニ（ベーシック/プラ）＋6mmハブ ×3
- 100mm ホイール円周 = π × 0.1 = 0.314 m/回転
- モータ **330 RPM** × 100mm → 330/60 × 0.314 ≈ **1.73 m/s（無負荷）**
- **不採用の理由**: 実際に採用したのは 60mm の Nexus 14145。
  上記の速度計算は現構成には適用されない。

### 旧案: Pololu #1443 を標準モータとする案（採用せず）

- Pololu #1443（30:1 37Dx52L, 350RPM, ストール 8 kgf·cm, 64 CPR, ¥15,000〜18,000）
- 350 RPM × 100mm ホイール → 5.83 × 0.314 ≈ **1.83 m/s（無負荷）**
- オドメトリ: 64 CPR × 30:1 = 1920 カウント/出力軸回転
- **不採用の理由**: 高価。まず安価な JGB37-520 で実測し、
  [§12 の改善順序](./drivetrain-spec.md#12-m2-で性能不足だった場合の調査順序)を経てから判断する方針に変更。
  **将来のアップグレード候補としては引き続き有効**（6mm Dシャフトのためホイール・ハブは流用可）。

### 旧案: BTS7960 ドライバ（採用せず）

- BTS7960（43A, 双方向）×3。「43A ≫ ストール電流」で余裕がある構成だった。
- **不採用の理由**: 試作1号機には過剰。安価な AE-TB67H450 を採用。
  ただし TB67H450 は余裕が小さいため、
  **ソフトウェアによるロック保護（[§5](./drivetrain-spec.md#5-モータロック保護ソフトウェア)）が必須**という条件付き。

### 旧案: 3S LiPo 2200〜3000mAh（採用せず）

- **不採用の理由**: 実際に採用したのは 1500mAh 35C。
  容量が不足した場合は [§12](./drivetrain-spec.md#12-m2-で性能不足だった場合の調査順序) の手順8で見直す。

### 旧: トルク・牽引の試算（100mm ホイール前提。**現構成には未適用**）

- 必要トルク（1輪あたり接地力 ~10N 想定）: 10N × 0.05m（＝100mmホイール半径）= 0.5 N·m ≈ 5 kgf·cm/輪
- 牽引限界: μ≈0.5、mg≈39N（4kg想定）→ 最大牽引 ~20N
- 結論だった「**トルクより牽引が先に限界**＝軽量化とグリップが効く」という定性的知見は**現構成でも有効**。
- ただし **60mm ホイール（半径0.03m）・JGB37-520（定格2.2 kgf·cm / 最大3.1 kgf·cm）では数値が変わる**ため、
  上記の数値をそのまま現構成に適用しない。**実測（M2）を根拠とする。**

---

## 3. 採用しなかった案: 部品・BOM

> 以下は BOM v0.1 の内容。**いずれも採用せず**、経緯の記録として残す。
> 価格は 2026年8月時点の参考値。

### 旧案: 既製の全方向ベースを購入する案（採用せず）

| 製品 | 参考価格 | 実測速度 | 判定 |
|---|---|---|---|
| Nexus Robot 3WDオムニキット（48/58/100mm） | （調査中のまま） | （不明） | 採用せず |
| 3WDオムニロボット（RobotShop・三角タイプ 10003） | ¥30,000前後 | 0.6 m/s / 積載15kg | 採用せず（速度不足） |
| メカナム系（OSOYOO / Yahboom / ヴイストン G120） | ¥9,000〜数十万 | 〜1.3 m/s | 方式非採用 |

**不採用の理由**: 安価な既製ベースは 0.6 m/s 級で、結局モータ換装が必要になる。
それなら**最初から部品を買って自作**する方が安く、寸法・重量・保護回路も自由に設計できる。

> なお v0.1 にあった「速度対策 策1〜3」のうち、**策1（投擲距離・方向を固定して必要横移動量を小さくする）**は
> 現在も有効であり、[NFR-1](./requirements.md#nfr-1-短時間移動性能-主評価軸) の
> 「初期MVPで許容する事項」として**システム設計に正式に組み込んだ**。

### 旧案: 駆動系の安価版BOM（採用せず／一部差し替え）

| 旧部品 | 現在 |
|---|---|
| Nexus **14049** 100mm ダブルオムニ ×3（¥6,000〜9,000） | → **Nexus 14145 60mm** |
| JGB37-520 12V **330RPM** ×3（¥4,500〜6,000） | → **JGB37-520 12V 530RPM** |
| 37Dφ用ブラケット 別途購入（¥1,000〜2,000） | → **モータ付属の金属ブラケットを使用**（¥0） |
| **BTS7960** ×3（¥1,800） | → **AE-TB67H450 ×3** |
| 3S LiPo **2200〜3000mAh** 20C以上（¥4,000〜6,000） | → **3S LiPo 1500mAh 35C** |
| 汎用バックコンバータ 12V→5V（¥1,000） | → **BP5293-50** |
| コネクタ・XT60・スイッチ・ヒューズ 一式（¥3,000） | → XT60M / 14AWG配線 / 10Aヒューズ |
| 駆動系小計 **約 ¥20,000〜25,000** | → 構成が変わったため**旧見積は現状に適用しない** |

### 旧案: アップグレード候補（現時点では採用せず）

- モータ → **Pololu #1443**（30:1 37Dx52L, 350RPM, ストール8kgf·cm, 64CPR、¥15,000〜18,000）。
  **不採用の理由**: 高価。まず JGB37-520 で実測し、
  [§12 の改善順序](./drivetrain-spec.md#12-m2-で性能不足だった場合の調査順序)を経てから判断する。
  6mm Dシャフトのためホイール・ハブは流用可 → **将来の選択肢としては有効**。
- ホイール → Nexus **14041**（ナイロン-ラバー、グリップ向上）。60mm 相当品の有無は要調査。

### 旧: 予算シナリオ（**参考**。現在の構成とは一致しない）

| シナリオ | 構成 | 概算合計 |
|---|---|---|
| エントリー | Kinect v2中古 + 既製メカナムベース + ESP32×2 + 電源小物 | ¥25,000〜40,000 |
| 推奨（バランス） | RealSense + 既製ベース + ESP32×2 + 電源 | ¥70,000〜90,000 |
| 本気（速度確保） | RealSense + ベース＋モータ換装 or 高性能ベース | ¥100,000〜 |

**現在の方針**: D435 ＋ 駆動系自作。上記シナリオ表は**過去の検討記録**であり、現構成の見積ではない。

### 旧案: 検出センサーの比較（D435 を採用）

| 製品 | 参考価格 | 3D取得 | 特徴 | 判定 |
|---|---|---|---|---|
| **Intel RealSense D435** | ¥35,000前後 | ◎ 直接 | **グローバルシャッター**で高速移動物体に強い。深度性能はD435iと同一。IMU無し | ★**採用** |
| RealSense D435i | ¥40,000前後 | ◎ 直接 | D435＋IMU。IMUはカメラ移動案でのみ有用 | 見送り |
| Kinect v2（中古） | ¥4,000前後＋PC接続アダプタ ¥3,000 | ○ | 激安。要Windows/SDK、depth 30fps、サポート終了 | 採用せず |
| Luxonis OAK系（OAK-D Lite/Pro） | ¥20,000〜 | ◎＋AI | カメラ内でニューラル処理 | 採用せず |

IMU の用途は「カメラを移動体に載せる案」でのエゴモーション補正のみ
→ 本プロジェクトはカメラ固定方針のため不要と判断。
再設置時の姿勢把握は**深度データからの床平面フィッティング**で対応する。

> 将来「ゴミ箱搭載案（対応範囲拡大）」に挑戦する場合はエゴモーション補正で IMU が要るため、
> その時点で D435i への置き換えを再検討する。

> 出典: [Switch Science: RealSense D435i](https://www.switch-science.com/products/5235) /
> [同 D435](https://www.switch-science.com/products/3633)

---

## 4. 採用しなかった案: 観測基盤・その他

観測基盤まわりで検討した案は、柱の採否と密接に結び付いているため
**[original-features.md §8](./original-features.md#8-検討したが採用しない案) に置いたままとする。**
概要のみ以下に示す。

| 案 | 不採用の理由（要約） |
|---|---|
| ROS 2 を導入し rviz / rosbag を使う | Pi 4 の資源制約に対して重い。加えて柱3 見送り・柱2b 保留の結果、肩代わりできる部分がほぼ残っていない。**先行事例として設計の参考にはする** |
| Grafana + Prometheus | 柱2b を作る場合には合理的。ただし Throw Record（1投擲 = 1エンティティ）はメトリクス時系列と相性が悪い |
| クラウドへの常時送信 | 屋内・単一ネットワークで完結するため不要。**ローカル完結とする** |
| 移動体へのカメラ搭載 | 固定側/移動体の責務分離を壊す。エゴモーション補正のため D435i が必要になり、D435 の選定根拠と矛盾する |
| スマホ（ブラウザ）からの緊急停止 | **Wi-Fi が落ちる状況＝まさに暴走しうる状況**で機能しない。非常停止は物理スイッチで満たす（→ [OQ-13](./open-questions.md#c-電源安全)） |
